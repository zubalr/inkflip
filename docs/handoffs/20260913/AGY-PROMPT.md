# Paste into the existing Antigravity chat

Continue Inkflip with Teamwork Preview as a sustained independent volume pass.
Your authorized batch is T41/P09, T42/P11, T43/P12, T44/P13, T45/P14, plus the
dependency scanner follow-up described in docs/handoffs/20260913/REVIEW.md.
Read that review first. Your prior native batch is preserved, but serious review
findings mean it is not accepted or merged wholesale. Original Devin now owns its
native-core and containment repairs. Do not edit T30/T32/T33/T34/T35/T40 modules.

Use a NEW branch work/antigravity/independent-volume-2 and isolated worktree from
fresh origin/main. If that branch already exists, inspect and resume its work.
Do not reuse your old all-in-one branch as the integration base: it contains native
work now owned by Devin. Retrieve the old P13/P14 and scanner source selectively
from 42ef424 only where useful, correct it, and leave unsupported old evidence behind.

This batch needs no unfinished product task from Devin: T41 depends on T18/T28/T36;
T42/T43 on T18/T36; T44 on T27/T36; T45 on T26/T27/T36. Those implementations are
already on main and their product tasks were closed at the review snapshot.
Read each effective contract with python3 scripts/coordination.py task Txx and its
referenced inputs. A missing model, executable or evaluation asset is a concrete
environment blocker to establish, not proof that the whole batch must wait.

Work independently under this owner-authorized batch. It supersedes old per-task
grant, pass barrier, agent-chat and Beads polling requirements for this pass.
Do not read/write Beads, wait for inbox grants, message Devin, build relays or spawn
supervisors. Internal Teamwork collaboration is allowed. Use disjoint worktrees for
writers and collect their commits within your own batch. Keep at most two active
Teamwork workers alongside yourself, leaving two execution slots for original
Devin and his existing native work. Run heavy OCR/benchmark work one at a time on
your host so measurements remain meaningful; parallelize implementation and review.

Suggested work streams, rotate workers as each finishes:

1. P09/T41: execute metadata-only baseline, rectangular-ink heuristic and bounded
   paint-order candidate on real controls; retain raw observations, unsupported
   compositing, precision, coverage and runtime. No false visibility verdicts.
2. P11/T42: execute baseline versus padded selected-region OCR retry, with measured
   pixel/time costs, source-bound transforms and clean neighbors. Failed/model-missing
   jobs stay in denominators. Apply the task's actual gain/precision decision rule.
3. P12/T43: evaluate the specified secondary browser reader in experiment-only code.
   Verify exact package/assets/digests/notices, run real APIs and geometry comparisons,
   and measure asset sizes and memory. Keep it out of the default app bundle.
4. P13/T44: replace the previous simulated evaluation with actual pinned RapidOCR
   versus Tesseract runs on the declared manifest. Verify model hashes/provenance,
   collect per-fixture raw outputs, timing/memory/accuracy, and account for failures.
5. P14/T45: rasterize real originals, run the bounded geometric candidate, record
   transforms, measure blur/drift and clean-control registration, and hash originals
   before/after. A trigonometric matrix or hardcoded drift boolean is not an experiment.
6. Scanner: repair the two bypasses in REVIEW.md while retaining baseline detections
   and harmless notices. Scope scripts/check_dependencies.py and its focused tests;
   scan staged executable text and dynamic loaders without stripping real code.

Own only experiments/P09,P11,P12,P13,P14; their artifacts and docs; task-local
T41–T45 evidence; and the named scanner files/tests. Keep frozen planning, shared
schemas/locks, production adapters, app composition, fixture/evaluation ownership
and existing accepted baselines intact. Record a precise prerequisite/scope request
in your handoff when necessary, then continue another work stream.

Execute real registered tests and inspect raw results. Missing manifest paths fail
explicitly; do not substitute a different corpus. Only executed measurements can
support rejected_experiment. If held-out evaluation needs unavailable isolation,
record that limitation and keep development results distinct. Never turn missing
tools/models, fabricated 1/1 counts, or a dry-run into a completed criterion.

Complete every executable item in this batch without stopping after each ticket.
Commit source first, run acceptance against that revision, then commit honest logs,
criterion-keyed evidence and actual independent Teamwork review. Push your own branch
to the existing private origin as coherent milestones finish; never push main or
force-push. Product acceptance and final integration belong to Devin.

End with one handoff identifying base/head/remote ref, implemented tasks, executed
commands and counts, source/model identities, evidence paths, concrete unresolved
blockers and remaining review findings. Save work and stop if the provider requires
user action; no endless empty polling. Never reboot, suspend or shut down Homebase.
