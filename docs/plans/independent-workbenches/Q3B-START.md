# E startup: unblock native CLI acceptance

Use the actual standalone Devin CLI with --model swe-2-max. Read current canonical
AGENTS.md, docs/NATIVE_PASSES.md, docs/HOMEBASE.md and E-START.md. Keep your existing
checkout and exact grant; do not dispatch yourself or mutate Beads. Read canonical
native_pass.py status devin --workbench E and select only pdf-q3b. No model fallbacks,
agent chat, subagents or extra supervisors for this grant.

You are an independent verification worker on the published setup base. Inspect
T27/T28/T29/T16/T22 existing implementation and peer reviews, then run each task's
actual effective acceptance commands with scripts/task_acceptance.py task TASK
--report artifacts/followups/pdf-q3b/TASK-run.json. Preserve exact evaluated commit,
commands, stdout/stderr and test counts. Install only locked project dependencies
in your isolated worktree if needed. Never modify source, shared schemas, locks,
Beads or accepted product receipts; disposable build outputs are not source edits.

Run checks sequentially within this checkout. Before real OCR/corpus work, check
pdf-pass1's heavy reservation; proceed only when explicitly held by pdf-q3b. If a
required environment or asset is unavailable, record that failure and continue
independent available checks. Do not report a skipped test or stale receipt as pass.

Inspect acceptance_receipts.validate errors against your exact HEAD and identify
which receipts need renewed commands, which need substantive source re-review,
and which criteria/manual evidence remain missing. Recent scripts/config changes
can invalidate dependent receipts; don't remove paths from the validator to hide
this. A alone records product acceptance from your evidence after independent review.

Commit only artifacts/followups/pdf-q3b/ with handoff.json and commands.log, recording
worker session identity, reviewed source commit, executed commands/counts, exact
remaining blockers and ready_for_review. Do not publish GitHub. Local commits are
the return transport; A reads them. When complete, yield cleanly so your slot can
start B/T30 or review the joined browser candidate. Never wait for agent chat.
