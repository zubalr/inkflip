# Workbench E: Independent verification and supply chain

Use Standalone Devin CLI / SWE-2.

Prioritize independent candidate reviews, then performance/parity, supply-chain rights, quality gate and final independent review. Cannot approve own changes.

Queue priority (choose first eligible, not necessarily first listed): pdf-q3b, pdf-cks, pdf-pfl, T39, T46, T47, T53, T55.

Read README.md beside this packet and your task contracts in roadmap.json. This is a standing lane brief, not proof of an active grant. Live task status/claims remain exclusively in Beads. At startup check whether the integrator has activated this roadmap and recorded this lane/session, exact task branch/base and scoped grant. If not, report activation-not-installed once and exit cleanly; do not spend hours polling an empty inbox or claim conflicting work. Canonical native_pass status supports --workbench E. Read current canonical scripts; older task checkouts may lack this option.

After activation: read your lane's published state and review artifacts at startup, after each checkpoint, and before choosing work. Use the first eligible task in your queue, skipping blocked tasks. Eligibility requires accepted merged dependencies with fresh evidence and an uncontested exact scope; never waive checks because a task is assigned to your lane. Do not wait for agent chat. Work in your own task checkout; preserve other writers and existing checkpoints. Keep original bytes, schemas, frozen planning, licenses and acceptance gates intact. Use the task's actual effective commands and record failures/skips honestly. Hand off an exact committed candidate plus existing task receipt/handoff and command logs. A checkpoint is not acceptance.

A alone changes shared Beads/acceptance and merges main. Publish a recoverable checkpoint, then work on another eligible item or service an independent review; do not start a task that depends on your unaccepted candidate. Review findings arrive as committed review files, not chat messages. Read and fix your own findings when received. Each grant records the actual branch/checkpoint transport; never guess or change the destination. Homebase uses the existing SSH Git/Dolt relay; no new Goal, broker or daemon.

Only A edits root wiring and shared execution/registry files; put proposed shared-file changes in docs/proposals/<task>-*.md with a concrete patch/reproducer. All workers together, including native descendants, remain within five active execution slots and lower provider limits. Pause your own writer before using its slot for an independent child. Keep existing single heavy-run reservation until measured capacity permits an explicit change. Every task gets distinct test ports, outputs and environment directories. Never reboot/suspend/shut down Homebase.

## Review priority
First review A's real public app journey and D's fixture semantics; then review B/C/D/A candidate checkpoints before starting long noncritical audits. Use detached review worktrees pinned to exact commits. Run only necessary meaningful affected checks, validate scope/contract rather than echoing worker summaries, and write the existing per-task review artifact. Fixes to production belong to the original lane. For your own implementation tasks, A schedules a different qualified reviewer from a yielded lane; you do not self-approve T47/T53/T55. Gate/evaluation custody remains separate from experiment authorship.

## Final review uses a fresh reviewer
T55 belongs to workbench E as an independent review slot, not to the E session that authored T39/T46/T47/T53 or product fixes. Before T55, commit E's evidence and yield its authoring session. A fills this same slot with a fresh nonauthor Devin review session, pinned to the complete merged release candidate, to execute the full T55 contract. It does not inherit author assertions as proof. A accepts only that full independent review and resolved findings. This replaces an occupied slot rather than adding a sixth concurrent worker.

Startup priority: pdf-q3b independently reruns T30 prerequisite checks after setup integration. Preserve existing review/criteria evidence; record actual results and remaining freshness failures for A to accept. Do not run optional experiments ahead of this unblocker.
