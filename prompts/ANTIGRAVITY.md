/teamwork-preview Build and verify Antigravity's portion of the next substantial Inkflip pass in this existing project. Use the already-open repository directory; do not create a new project under teamwork_projects. Use development integrity mode because this product intentionally uses audited open-source dependencies. The owner approves the scope in this prompt and existing brief; proceed through the native scope step and autonomous team workflow, honoring any required native approval UI.

Read AGENTS.md, START_HERE.md and docs/NATIVE_PASSES.md. Fetch origin/main, use the existing shared canonical Beads database and `python3 scripts/native_pass.py status antigravity`, and capture the current pass as the target. Devin Local is the sole state writer and integration lead. Wait for its published assignments; prepare/resume assigned branches at their recorded bases. Isolate concurrent writers. Do not claim Beads tasks, initialize a second database, use historical task-start commands or merge into main.

Use native Teamwork orchestration for the UI, accessibility and visual tasks in execution/passes.json. At most TWO Antigravity product workers/reviewers may be active, including your active implementation/review and automatic critics/auditors. A worker yields before review takes its slot. Preserve independent review and real browser evidence. Publish commits, executed evidence and task-local handoff.json on each assigned branch; incorporate feedback and continue newly assigned work without another owner ticket prompt. While dependencies are pending, do independent work first, then poll at bounded intervals.

Complete when every Antigravity task in the captured pass is accepted on origin/main with no requested revisions. One finished ticket or a temporarily empty inbox is not completion. Resume ordinary errors/context compaction. The owner approves private repository access, source sharing with this harness, worktrees, commits and pushes; Devin owns integration. Keep Bun isolated, React/Vite/CSS Modules and central tokens. Preserve planning/notices; do not add Effect, weaken gates, fabricate checks or reboot Homebase. Report accepted work, actual visual/accessibility checks and concrete blockers.
