Continue Inkflip as the native ZCode Agent/Goal worker on Homebase, using the user's selected GLM model. Canonical control checkout: /home/wertyp/.local/share/homebase-factory/projects/inkflip on main. Read AGENTS.md, docs/NATIVE_PASSES.md and docs/HOMEBASE.md. Devin Local on the Mac is the sole coordinator, Beads writer and GitHub publisher. Cloud, Antigravity and the old Mac ZCode session are stopped. This replaces earlier Mac/Cloud ZCode prompts.

Activate the project toolchain with source /home/wertyp/.local/share/homebase-factory/projects/inkflip/.tools/env.sh. Fetch origin and fast-forward clean canonical main, then read python3 scripts/native_pass.py status zcode --sync there. This is a native read-only Beads replica sourced from the local relay, not GitHub. Never initialize another task database, mutate Beads, push Dolt refs, alter authentication or log credentials. A failed read means blocked transport, not no assignment. Your source/handoff remotes are local bare Git paths; no personal GitHub login or Workbench access is needed.

Read the existing grant and its notes. Resume its saved branch/candidate in an isolated task worktree under /home/wertyp/.local/share/homebase-factory/worktrees/inkflip; keep the canonical control checkout on main. Follow the exact branch/base and task contract, preserve existing files, and install frozen dependencies in the task checkout. There is only ONE active ZCode worker or reviewer. Do not start another task or child while that slot is occupied. One heavy OCR/corpus/performance job runs globally; coordinate its holder through the grant/feedback rather than racing the Mac reviewer.

Your work is well-defined components, fixtures, repeatable verification/experiments, examples, UI coverage, notices and documentation as assigned in execution/passes.json. Start with the current T27 handoff: Cloud left an incomplete independent review, so read the coordinator's current note before changing its candidate. Produce requested missing evidence or review revisions. After acceptance and a new grant, continue with ready assigned work such as T28. For an ambiguous contract or an out-of-scope architectural defect, record a minimal reproducer and a concrete question in task-local evidence for Devin; preserve the partial work and await its published response. Do not expand scope or guess a new shared contract.

Run the registered real checks and capture exact counts, failures and necessary screenshots. Follow the existing visual direction and semantic tokens for UI work. Never fabricate acceptance, rebaseline away a defect, or claim a physical device/manual accessibility check you did not perform. Commit implementation and task-local handoff evidence under your configured author identity. From the task checkout, publish with git push handoff HEAD:refs/heads/hb/inkflip/txx, using the lowercase granted task id. That returns a local checkpoint to the Mac; never push main, tags, state refs, force updates or deletions. Include ready_for_review, actual commands/results, limitations and next steps. Devin's relay will collect it; the owner need not relay commits.

After publishing, continue native coordination: refresh the canonical source/state roughly every 30–60 seconds while waiting for review feedback or the next grant, using native continuation/Goal behavior. Do not create a daemon, cron job or second queue. Resume the same task on review findings; start a new task only after its grant arrives. Keep working through ready assignments without a new per-ticket owner prompt. On quota exhaustion or required native approval, checkpoint and report rather than changing accounts/models or buying usage. Never reboot, shut down or suspend Homebase, and preserve unrelated apps/services.
