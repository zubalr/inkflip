Continue Inkflip in native ZCode Goal mode on Homebase with the selected GLM-5.3-Flash Max model. Codex/Astra on the Mac is the sole orchestrator, Beads writer, GitHub publisher, integrator and acceptance authority. Devin Local SWE-2 is a peer implementation swarm, not the coordinator. This replaces all previous Devin-led, Cloud and Mac-ZCode prompts.

Use canonical control checkout /home/wertyp/.local/share/homebase-factory/projects/inkflip on main. Activate source /home/wertyp/.local/share/homebase-factory/projects/inkflip/.tools/env.sh. Read current AGENTS.md, docs/NATIVE_PASSES.md and docs/HOMEBASE.md from canonical main. Fetch origin, fast-forward only clean main, and read python3 scripts/native_pass.py status zcode --sync there. This is a native Beads read replica sourced from the local Git relay. Never mutate Beads, bootstrap another database, push Dolt refs, alter authentication or log credentials. Failed sync is blocked transport, not no assignment.

Run continuously through useful granted work, preserving native Goal continuation and session resume. Grants and feedback come from Astra through Beads; the owner does not need to paste a new prompt per task. Goal completion must follow actual accepted outcomes. Provider quota, native approval or an unavailable dependency is a specific blocker to checkpoint honestly, not a reason to invent completion or switch accounts/models.

Use an isolated worktree under /home/wertyp/.local/share/homebase-factory/worktrees/inkflip for each writer. The saved T27 worktree is pdf-t27 on work/zcode/t27 at c884b59fee2c470233a6ee0f9f2207cc2d21c6cb. Preserve this candidate and the stopped Cloud review. Read Astra's latest note before changing or running its OCR suite. First inspect the handoff and prepare requested missing evidence or concrete revisions; an independent SWE reviewer owns review and Astra owns acceptance. The initial startup message specifies the resource holder and concrete first work.

The owner removed fixed worker limits. Use enough native workers to handle granted independent scopes when that improves progress and the app supports them. Every descendant needs a named role, isolated writing checkout and coordinator-visible allocation. Do not recursively spawn supervisors or dispatch yourself. Serialize work on one branch. Run only one heavy OCR/corpus/performance job across the project at a time, following Astra's current Beads reservation; other independent work may continue. Give concurrent servers unique ports and generated paths.

Handle well-defined components, fixtures, repeated verification, experiments, examples, layout coverage, notices and documentation as assigned. Shared-contract or architectural uncertainty goes to Astra as a minimal reproducer and concrete question in task-local evidence. Run actual registered checks, preserve raw text and geometry contracts, and report failures and missing device/manual evidence honestly. Do not weaken gates or fabricate review results.

Commit implementation and handoff evidence under the configured author identity. Return each checkpoint with git push handoff HEAD:refs/heads/hb/inkflip/txx from its granted work/zcode/txx checkout. Include exact implementation_commit, ready_for_review, real commands/results, limitations and requested revisions. The reviewed hook permits only this task-local transfer; never push main, tags, state refs, delete branches or force updates. Astra collects the exact commit and decides review/integration.

While awaiting feedback or a new grant, refresh canonical source/state with native bounded waits around 30–60 seconds. A temporarily empty queue is not the end of the Goal. Do not create a daemon, cron job or second task queue. Keep making progress within grants and resume the same task when feedback arrives. Never reboot, shut down, suspend or power-cycle Homebase; preserve unrelated apps and services.
