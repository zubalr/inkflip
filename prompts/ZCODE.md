/goal Complete ZCode's portion of the next substantial Inkflip pass in this existing repository using the selected GLM model. Read AGENTS.md, START_HERE.md and docs/NATIVE_PASSES.md. Use native persistent goal execution and bounded native subagents where useful. Capture the current pass from `python3 scripts/native_pass.py status zcode --sync` as this run's target; continue until all ZCode tasks in that pass are accepted on origin/main with no pending revisions. Do not stop after a single task or require a new ticket prompt.

Devin Cloud is the sole Beads writer and integration lead. Fetch origin/main, read published assignments and create/resume their branches at recorded bases. Use the existing canonical Beads replica, not a second database or JSON tracker. Do not claim tasks or use historical start commands. Preserve dirty work and commits. Use ONE active ZCode worker/reviewer at a time: delegate sequentially and yield while a subagent works. Independent review may use a distinct session after the writer yields or the integration lead's available reviewer.

Own the fixture, native reader, corpus and experiment tasks in execution/passes.json. Run real acceptance and regression checks, commit evidence and handoff.json, then push each assigned branch. Incorporate feedback and continue unblocked assignments. Wait at bounded intervals for integration. Keep held-out labels outside the implementer's accessible environment; never fabricate Linux, device, benchmark or manual evidence.

The owner approves private repository access, source sharing with this harness, worktrees, commits and pushes; Devin owns integration/acceptance. Keep Bun isolated, React/Vite/CSS Modules and Python/uv; do not add Effect or weaken gates. Preserve planning, original bytes, notices and immutable baselines. Never reboot/suspend Homebase. Use normal goal resume for context/quota interruption. Report accepted tasks, measured experimental decisions, actual checks and owner-dependent blockers.
