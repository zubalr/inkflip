# T27 current criterion evidence

Current merged candidate `da1efb6125e4a4f45c75a09ce889bbe06b8736e9` contains reviewed source `2ea6233` byte-for-byte. The actual Linux registered run passed all 58 cases and 20 separately reported subtests with no skipped or failed cases. Full native verification passed 111 cases and 146 separately reported subtests. Current run identity and counts are in `run.json`; the final section of `commands.log` records the merged checks and cleanup.

The independent review chain is `wave3-full-review.md` (full review with three findings on the prior source) followed by `wave4/independent-review.md` (all three resolved, current source approved with 46 light cases, 20 subtests and independent probes). The final real-engine checks were performed by ZCode after that approval and are not represented as independently rerun by Erdos. Current source/tests are unchanged by the merge; reviewed shared dependency/geometry/report changes have their own committed evidence.

| Exact criterion | Executed checks in the 58-case registered suite |
| --- | --- |
| No URL or runtime model fetch | `test_model_hash_is_the_exact_local_traineddata`, `test_explicit_prefix_model_is_hashed_and_pinned_in_argv`, and `test_invocation_uses_only_neutral_generated_names`; adapter source inspected by the independent reviewer. The model is a local installation file whose actual hash is reported. Native Linux's installed model may differ from the browser model; no cross-platform equality is claimed. |
| missing model/binary/unreadable page differ | `test_missing_binary_is_unsupported`, `test_missing_model_is_distinct_from_missing_binary`, `test_unreadable_pixels_fails_at_open`, `test_blank_raster_completes_with_zero_occurrences`, and `test_timeout_is_a_distinct_terminal`. |
| malicious filename cannot become an option | `test_language_identifier_cannot_smuggle_flags` and `test_invocation_uses_only_neutral_generated_names`; fixed argv and neutral generated image paths are inspected. Validated caller language identifiers are passed as argument values; this does not claim no caller-derived value ever reaches argv. |
| crop padding/resize inverses correct | `test_padding_is_max_of_8px_and_ten_percent_clipped`, `test_single_line_psm7_reads_amount_through_inverse_transform`, `test_odd_resize_inverse_uses_actual_destination`, `test_contract_numeric_example_recovers_56_340`, `test_f08_fiducial_all_four_rotations`, and `test_shear_quad_is_not_axis_aligned`. Independent closed-form/pixel anchors and three geometry mutation controls are recorded in the review chain. |
| native OCR retains raw punctuation and all occurrences. | `test_raw_punctuation_and_case_are_retained_verbatim`, `test_f03_scan_ocr_reads_pixels_only_and_ignores_invisible_layer`, `test_every_occurrence_and_score_validate`, `test_257_valid_words_survive_later_malformed_row`, and the exact/over-5000 occurrence-budget pair. Engine misreads and punctuation remain raw; limits and malformed rows retain prior complete evidence and a typed terminal result. |

Printed-English OCR is the implemented profile. Missing F14/F15 families remain recorded fixture follow-ups; these criteria do not imply multilingual OCR. Parent supervision and containment belong to T29/T40; this adapter's process checks do not establish the future hardened-container boundary. The earlier root `commands`, `environment`, and `criteria` sections in the worker receipt are retained as dated history; the normalized criterion map below and this document describe current evidence.
