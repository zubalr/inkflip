# T27 independent source review — wave 2

**Verdict: changes requested; source is not yet acceptable.** Reviewed exact HEAD `c14e4b0313fcb614f3a85aa50df045b55c7bdd55` on `review/codex/t27-wave2`, including revision `12bc8acca01aa2faa3948d3c60df43eefcbf5c57`. Reviewer: Erdos, independent Codex session `01a093f7-2dea-78e3-80cc-c495153d946c`. Checkout was clean. Read-only review of `native/inkflip/readers/tesseract.py` and `native/tests/ocr/test_ocr.py`, effective T27, repository guidance, reader/coordinate/invariant contracts, and canonical SWE preliminary findings. No checkout/Beads edits, commits, pushes, real OCR, model loading, corpus or browser suite.

## Actionable findings

1. **[P2] Invert the resize actually performed, not the requested factor.** `tesseract.py:491–495` rounds destination dimensions, but `:113–115` and `:557–560` invert the original factors. A 101×51 raster resized by `(0.5,0.5)` becomes 50×26. Canned TSV box `(0,0,50,25)` produces canonical corners `(0,0)`–`(100,50)` at scale 1; the actual resize maps those corners to `(0,0)`–`(101,49.038462)`. This violates T27's explicit correct-resize-inverse criterion. Record/use effective factors `(target_width/source_width, target_height/source_height)`. Test an odd-sized crop and fractional resize against the actual destination dimensions. Existing `test_ocr.py:227–239` checks only algebra at exact 2× without exercising image resize, so cannot detect this.

2. **[P2] Bound the resized raster before allocation.** `tesseract.py:327–335` allows any finite positive factor; `:493–495` has no output pixel-budget check. A tiny 101×51 input with `(1000,1000)` calls Pillow resize with **101000×51000 (5.151 billion pixels)** despite `MAX_RASTER_PIXELS=40M`. The probe intercepted the call before allocating. This is an adapter-owned pre-subprocess allocation, not T29 process supervision. Validate rounded destination dimensions/pixel count first and return typed `resource_limit`. Add a mock-based test proving `resize` is never called above budget. F6's input-header fix is correct but does not cover this path.

3. **[P2] Complete the typed-failure boundary.** `tesseract.py:323,327` still raises raw `TypeError` for caller `CropPlan(psm=[])` and raw `ValueError` for `resize=(1,)`; `extract` catches only `AdapterError` at `:464–472`. Separately, model `Path.read_bytes()` at `:189` can raise `PermissionError`/`OSError`, escaping the catch at `:474–478`. Both were reproduced; model I/O was injected, without reading any real model. Validate crop field shape/type before membership/unpacking and translate model-read failures into a sanitized typed terminal. Tests `:438–472` cover a hostile string, zero/NaN factors and bounds, but not malformed field shapes; F12 tests cover only workdir/save faults. This is the existing failure-isolation contract, not a request for a new supervisor.

4. **[P2 / F10] Scale-only is not yet a safely enforced geometry contract.** `tesseract.py:289–292,370–378` consumes only scale; `:117–121,559–569` labels division by scale as canonical geometry. A small raster with metadata `{raster_scale_px_per_pt:1, rotation:90}` completes with the same polygon as rotation 0, ignoring rotation. Under `COORDINATES.md:30`, canonical recovery must undo R as well; its numeric example requires `(56,340)`, whereas scale-only recovery of raster `(540,84)` at 1.5 gives `(360,56)`. There is no defined/enforced metadata precondition establishing that incoming rasters are unrotated canonical rasters. Tests render the original PDF orientation (`test_ocr.py:45–57`) but exercise only geometry-0, not rotated anchors. The receipt's scale-only limitation does not make misplaced polygons safe. **Coordinator decision:** either formally restrict and enforce this API to verified canonical-unrotated named rasters, returning unknown/null geometry or a typed refusal for unsupported/unproven transforms, or support the recorded inverse render transform. Rotation support need not be added to T27 if the restricted contract is explicitly approved and safely enforced. This is a source/contract gap, not merely stale metadata.

5. **[P3] Repair the per-language fallback regression fixture.** `test_ocr.py:621–632` places binary at `root/tesseract` and the alleged first candidate at `root/share/tessdata`. Source `tesseract.py:157–160` actually probes `root.parent/share/tessdata`, then `root/tessdata`. Thus the decoy is never inspected and the green test does not prove “existing first directory lacking language does not shadow the second.” Put binary at `root/bin/tesseract`, decoy at `root/share/tessdata`, and target at `root/bin/tessdata`; remove `TESSDATA_PREFIX` within this fallback-only test. Independently probing that corrected layout confirmed the current source fallback works, including a symlinked binary. This finding concerns regression fidelity, not a demonstrated fallback defect.

## Confirmed fixes and executed evidence

Tiny 101×51 in-memory PNG; fake on-disk model bytes; mocked subprocess returning canned TSV. CPython 3.13.15 / Pillow 12.3.0 from frozen offline dependencies in `/private/tmp/inkflip-t27-review-venv`. Initial sandboxed uv cache access failed; approved offline retry succeeded. No real engine was invoked.

Executed (both exit 0):

- `PYTHONDONTWRITEBYTECODE=1 /private/tmp/inkflip-t27-review-venv/bin/python /private/tmp/inkflip-t27-wave2-probes.py`
- `PYTHONDONTWRITEBYTECODE=1 /private/tmp/inkflip-t27-review-venv/bin/python /private/tmp/inkflip-t27-wave2-controls.py`

Confirmed F1 language `deu` reaches `-l`; `--tessdata-dir` and SHA-256 refer to the same fake language file. An explicit prefix missing that language fails without fallback. Correctly constructed per-language/symlink fallback works. F2 rejects unresolved region_id; F3's six malformed-region controls raise typed errors; F5 ids differ across pages; F6 rejects oversized declared dimensions before decode (mock header, no large allocation); F7 emits exactly 5000 for both 5000/5001 rows, with completed/resource_limit respectively. F12 workdir/save OSErrors are typed. Fixed argv has `shell=False`; raw `$100.` survives. F4 is improved but incomplete as above.

These are targeted probes, **not execution of the reported 36 OCR / 89 native tests**. Source/test inspection supplies the remaining assessment. No full verify was run: coordinator owns merging `500abfc` and the fixed 115-test harness. Known stale handoff fields were not re-audited. F8 stdout/process-group/cancel remains assigned to T29. Final verification and acceptance binding must follow the merged source+harness candidate; this report cannot certify them.

## Narrow follow-up — TSV boundary and F10 direction

**F3 extension, proven:** `tesseract.py:397` raises `AdapterError` for a malformed TSV header during iteration at `:545`; the surrounding `finally` (`:625–626`) cleans up but does not produce a terminal result. Stub stdout `unexpected\theader\n` escapes as raw `AdapterError(parser_error)`. Numeric probes: confidence `nan`, `inf`, `-inf` completes with scores **1.0, 1.0, 0.0**, respectively; strict `json.dumps(..., allow_nan=False)` succeeds. Thus **no nonfinite JSON leak was demonstrated from these TSV values**, but invalid diagnostics are silently promoted/clamped into valid confidence. A coordinate token `inf` drops the row and reports completed/zero; a 309-digit integer coordinate escapes as raw `OverflowError` during geometry conversion. Validate finite confidence and bounded/valid numeric word geometry, return a typed parser failure for malformed engine rows instead of silently claiming complete coverage, and catch adapter parsing/conversion errors around consumption of the generator. Preserve already emitted count/ids if a later row fails. This is adapter output parsing, not T29 supervision.

Executed `/private/tmp/inkflip-t27-tsv-followup.py` with the same frozen temporary Python and `PYTHONDONTWRITEBYTECODE=1`: exit 0, six mocked-TSV cases, no engine/model load.

**F10 advice:** the proposed optional owned `canonical_to_raster` matrix is a coherent minimal correction. No shared schema/planning change is needed for this Python API. Material details:

- This matrix maps **canonical** to raster: for the documented render it is `Scale(s) * R`, not PDF-user-to-raster `P = Scale(s) * R * C`. Do not apply C twice. The numeric example must recover `(56,340)` from raster `(540,84)`.
- Snapshot validated handle metadata into immutable numeric tuples at open; derive its inverse once. Missing matrix means unknown/null geometry regardless of legacy scalar. An explicitly invalid matrix should fail typed, rather than silently becoming an apparently valid scalar path. Append the optional CropPlan field to preserve positional construction, but derive/check it against handle provenance at extraction; a stale or caller-invented inverse must not win.
- Existing `inkflip.contracts.inverse` (`core.py:295–305`) checks only determinant magnitude. The owned boundary must additionally check finite/nonbool six components, finite determinant/inverse/results, and round-trip error over the actual raster extent. Translate `ContractError` and arithmetic failures into typed geometry failure. Keep full precision internally; round only serialized results and normalize negative zero.
- Undo the **actual rounded** resize and padded crop offset, then apply the handle inverse independently to all four TSV corners. Keep the quad; do not rebuild an axis-aligned box. The crop-output budget remains required before Pillow allocation.
- Recorded actual F08 render transforms and independent anchors across four rotations are appropriate. Add one tiny shear/general-affine canned-box test: quarter-turn rotations alone cannot distinguish correct quad mapping from a two-corner/bounding-box implementation. Matrix algebra alone cannot prove render provenance; the independent rendered anchor is essential. Test missing-matrix scalar-only and caller-provenance mismatch negatives.

Document this owned API decision in `docs/proposals/T27.md` through the coordinator/sole writer. This reviewer made no source changes. A rotation-specific switch would be smaller in lines but would not faithfully implement the proposed six-component affine contract.
