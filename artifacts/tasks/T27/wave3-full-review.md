# T27 independent full review — wave 3

**Verdict: changes requested on exact `1e7c8db8ff9152c36d0a7289e310b499fb6ac762` (source `5ba110b0a3533b008d03c4ba0e5000ae5c6c8f43`).** All required executed suites pass, and the affine/rounded-resize implementation passes independent anchors. Three bounded source/test findings remain. This is a full Linux source-and-execution review, not source-only approval or merged-branch acceptance.

Reviewer: Erdos, independent Codex `01a093f7-2dea-78e3-80cc-c495153d946c`. Mac checkout: `/Users/zubair/Code/Projects/pdf project/worktrees/review-codex-t27-wave3`; separately cloned Linux checkout: `/home/wertyp/.local/share/homebase-factory/worktrees/inkflip/review-t27-wave3`, detached at the exact candidate from the local relay. Both remain source-clean. Read effective T27, repository coordination/acceptance guidance, current `pdf-t27` Beads notes (including approved F10 decision and sole heavy reservation), reader/coordinate/invariant contracts, full adapter/tests, and prior review. No source/Beads edits, commits, pushes, service operations or writer-checkout use. The artifacts-only `4eed963` is outside this immutable review.

## Remaining actionable findings

1. **[P2, previous F3] Truncated word rows still silently lose coverage; invalid box extents still earn estimated geometry.** `native/inkflip/readers/tesseract.py:539–540` skips any short TSV row before validation. With header `level\tconf\ttext\tleft\ttop\twidth\theight`, stdout row `5\t90\tlost\t0\t0\t1` returns **completed, zero occurrences**. This contradicts the approved requirement to fail malformed engine rows without overstating coverage. At `:562–565`, coordinate checks enforce magnitude only: width 0 returns a degenerate four-point polygon, width -1 a reversed box, both completed/estimated. Validate word-row completeness and positive box extents before emission; return a typed parser terminal for malformed rows and preserve earlier evidence. If retaining text with unusable boxes, use the contract's unknown/null geometry rather than estimated invalid geometry. Add short-row and nonpositive-extent controls, including after a valid chunk. Existing `test_ocr.py:531–563` covers numeric/header failures but not these cases.

2. **[P2, previous F3] Typed input/allocation boundary remains incomplete.** Proven with the tiny probe:
   - `tesseract.py:480`: caller `dataclasses.replace(crop, canonical_from_raster=42)` escapes as raw **TypeError**, because it converts to tuple before shape validation.
   - `:167–172` / `:188` / `:435`: `10**309` as a matrix component or resize factor escapes as raw **OverflowError** from `math.isfinite`; it is a Python int and passes the preceding type check.
   - `:657–658`: Pillow resize now runs outside the preceding image-error boundary; an injected `OSError` on a valid 2× resize escapes raw. This is a regression from the prior crop/resize try block, separate from T29 supervision.
   Validate the new optional inverse field before conversion, make finite-number conversion reject unrepresentable ints, and translate expected resize allocation/I/O failures into sanitized typed results. Do not introduce a blanket catch for programming errors. The newly added malformed-field test (`test_ocr.py:979–996`) exercises only PSM-list and short-resize inputs, so these gaps remain green.

3. **[P2, regression fidelity] The rotated-render regression does not check the returned coordinates.** `test_ocr.py:903–912` computes expected canonical points, but checks only their finiteness/bounds and separately checks that emitted points are finite. It never compares the two. An in-memory mutant adding **10,000 physical points to every emitted x/y** passes `test_all_four_rotations_with_real_tiny_renders`, including its three rotation subtests. The numeric-example test correctly kills that same mutant. Also, the named “all four” test only OCRs 90/180/270; 0 is used as a bitmap reference, and the fixture is F03 scan-correct rather than the requested F08 known fiducial. Assert every emitted corner against independent expected coordinates for all four rotations, tied to actual rendered anchors. The reviewer’s separate F08 UserUnit-2 fiducial probe passes all four rotations on current source, so this finding is a missing regression assertion, not evidence that current quad mapping is wrong.

## Actual verification

All commands ran in the separate Linux checkout with the existing project `.tools/env.sh`; no global installation/configuration changes. Offline frozen dependency setup succeeded. Versions: Linux x86_64, CPython 3.13.15, uv 0.12.13, Bun 1.4.0, Pillow 12.3.0, pypdf 6.18.0, pypdfium2 5.8.0, Tesseract 5.5.3 / Leptonica 1.87.0.

| Executed command | Result |
| --- | --- |
| `uv run --offline --frozen --project native python -m pytest native/tests/ocr -q --junitxml=/tmp/inkflip-t27-wave3-logs/ocr.xml` | **52 passed + 13 subtests**, exit 0, 7.03 s |
| `bun run test:native` (registered frozen full-native suite) | **105 collected / 105 passed / 0 failed / 0 skipped**, exit 0, 7.89 s |
| `python3 scripts/task_acceptance.py task T27 --report /tmp/inkflip-t27-wave3-logs/acceptance-run.json` | **52/52, 0 failed/skipped**, 13 subtests in raw output, exit 0; record binds exact `1e7c8db` |
| `bun run verify` | **115/115, 0 failed/skipped**, exit 0; fixed shared harness included |
| `PYTHONDONTWRITEBYTECODE=1 uv run --offline --frozen --project native python /tmp/inkflip-t27-wave3-logs/probes.py "$PWD"` | Bounded independent probes completed, exit 0; expected defect observations/mutation failures recorded explicitly |

Independent probes establish: actual F08 `userunit-2.pdf` fiducial ink at the expected raster location and exact four-corner recovery at 0/90/180/270; 257 valid duplicate/punctuated words and their ids survive a subsequent malformed-confidence row; NaN/+Inf/-Inf confidence are rejected without nonfinite JSON. In-memory mutants demonstrate that the shear test kills two-corner rectangle reconstruction, the odd-size resize test kills inversion by requested factors, and the numeric-example test kills displaced geometry. The rotated test alone survives the displacement mutant described above. No repository files were mutated.

The real OCR suite and source confirm the earlier language/directory pinning, explicit-prefix refusal without fallback, corrected per-language fallback fixture, neutral fixed argv / `shell=False`, distinct binary/model/pixel/timeout outcomes, unknown/null geometry without matrix, crop provenance mismatch refusal, input/output budgets, exact 5000-word cap, page-scoped ids, and raw punctuation/occurrence preservation. The approved affine model and rounded output factors are implemented correctly for the tested valid cases. No runtime model/URL-fetch path was found by source inspection; network traffic was not separately instrumented.

Actual native model: `/usr/share/tessdata/eng.traineddata`, SHA-256 `daa0c97d651c19fba3b25e81317cd697e9908c8208090c94c3905381c23fc047`. This is the previously documented native-model divergence from planning's browser tessdata-fast `7d4322bd…`, not evidence of using the browser model. The adapter reports this actual local hash and passes that directory/language to the engine. Source SHA-256 `5c0d7c2e77d8734986e546226dca5565ceb21f25b37ecd825c1e35f2cc5f8102`; test SHA-256 `aa00219811df003290b03acb8e336a9c9c31af094877769901e46a25661742f2`. Full source/test/fixture/tool/model hashes are in the final identity log.

Raw logs and structured records: `/private/tmp/inkflip-t27-wave3-logs/` (`ocr.log`, `ocr.xml`, `native.log`, `native-counts.json`, `acceptance.log`, `acceptance-run.json`, `verify.log`, `verify-counts.json`, `probes.log`, `environment.log`, `final-identity-hygiene.log`). Reproducer: `/private/tmp/inkflip-t27-wave3-probes.py`. Their Linux originals are under `/tmp/inkflip-t27-wave3-logs/`.

Known handoff-summary corrections remain parent-owned. F8 stdout/process-group/cancel remains T29; no new supervisor work requested. ZCode owns source revisions; final acceptance must bind the reviewed fixes and fresh merged checks, never this candidate's green suites alone.

**HEAVY OCR RESERVATION RELEASED.** All review commands have exited; final process check found no running Tesseract or pytest/probe processes. No heavy run is queued by this reviewer. No further execution is planned for this candidate.
