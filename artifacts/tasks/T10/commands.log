T10 commands.log — devin-cloud-t10 (Devin Cloud session 5641af15b25d46e49f2dd6e2c76c3293)
Stopped mid-task by coordinator (Cloud run ended for quota). All counts below are real
executed results; the suite is NOT green — do not treat this as acceptance evidence.

# Toolchain (pinned, verified on this VM)
node v22.23.2 | bun 1.4.0 | uv 0.12.13 | python3 3.13.15 (installed under ~/.local/bin)

$ git checkout -b work/devin/t10 origin/work/devin/t10   # head f5a151e
exit 0
$ bun install --frozen-lockfile
exit 0 (87 packages)
$ bun x playwright install chromium
exit 0 (Chromium Headless Shell 143.0.7499.4)
$ uv sync --frozen --project native
exit 0
$ bun x --no-install tsc -b tsconfig.json
exit 0

# Real harness runs
$ bun run test:browser -- tests/readers/tesseract.spec.ts    @ f5a151e
exit 1 — collected=11 passed=2 failed=9 skipped=0
  All 9 failures identical: page.evaluate TypeError
  "Cannot perform %TypedArray%.prototype.set on a detached ArrayBuffer"
  in harness loadDoc — pdf.js getDocument() transfers/detaches the input
  ArrayBuffer before contracts.sha256(bytes) runs. Harness-seam bug, not
  adapter behavior. Fixed by 3fdfb0f: pass pdf.js bytes.slice().

$ bun run test:browser -- tests/readers/tesseract.spec.ts    @ 3fdfb0f
exit 1 — collected=11 passed=10 failed=1 skipped=0
  Passing: real PSM-7 crop OCR w/ identity + same-origin-only requests;
  PSM-6 page OCR + worker reuse + deterministic ids; init_crash vs
  unreadable_pixels; model_integrity + corrupted-cache rejection;
  offline distinct; run pixel budget; geometry/confidence semantics;
  cold/memory/cached + explicit removal; transient retry + cancel.
  Failing: 'bounded raster/edge/pixel counts enforced and recorded' —
  spec.ts:934 expect(outWidthPx).toBe(floor(cropWidthPx * resizeK)):
  outWidthPx=1758 but floor(4896*resizeK)=1757. Real adapter defect in
  packages/readers-tesseract/src/crop.ts planCrop: recorded
  resizeK=round6(min(outW/cropW,outH/cropH)) falls below the width
  realization. Next worker: record resizeK inside
  [max(outW/cropW,outH/cropH), min((outW+1)/cropW,(outH+1)/cropH))
  (midpoint + floor check) so floor reproduces outW/outH exactly, then
  rerun the suite; then task_acceptance --report run.json, receipt.json.

# ---------------------------------------------------------------------
# Session 2 — devin local SWE-2 subagent (coordinator: Astra)
# Worktree /Users/zubair/Code/Projects/pdf project/worktrees/pdf-t10,
# branch work/devin/t10. Local commits only; no push, no Beads writes.
# Toolchain here: node v26.7.0 (~/.local/bin), bun 1.4.0.
# LIGHT CHECKS ONLY — ZCode holds the global heavy OCR reservation;
# no playwright/OCR/model-load commands were run. Port 5191 reserved
# for the deferred heavy runs.
# ---------------------------------------------------------------------

$ git status && git log --oneline -5
exit 0 — clean at 61dbc3d on work/devin/t10.

$ git fetch origin
exit 0 — origin/main at 65e84da (includes merged T09 + T06/T07 evidence).

$ git merge origin/main            # coordinator-authorized integration
exit 0 — clean merge, 0 conflicts -> merge commit 50dddc0
  (+133 files: readers-pdfjs package, pdfjs.spec.ts, T06/T07/T09
  acceptance evidence, homebase scripts, bunfig/bun.lock toolchain).

$ bun install --frozen-lockfile
exit 0 (4 packages installed; lockfile untouched)

$ bun x --no-install tsc -b tsconfig.json     # post-merge, pre-fix
exit 0

# Reproduce BEFORE fixing — arithmetic, then real planCrop.
$ node -e '<inline arithmetic: 4896x6336, caps 4e6/8192>'
exit 0 — realized outW=1758 outH=2275 at k=0.3590886736076522;
  recorded round6(min(1758/4896, 2275/6336)) = 0.359059 lies BELOW the
  feasible interval [0.3590686, 0.3592172): floor(4896*0.359059)=1757,
  floor(6336*0.359059)=2274 — BOTH axes misreproduce.

$ node artifacts/tasks/T10/repro-planCrop.mjs   # pre-fix @ 50dddc0
exit 1 — defect reproduced through real planCrop, 5/6 cases misrecord:
  4896x6336  rec 0.359059 -> floor 1757x2274 (realized 1758x2275)
  9000x120   rec 0.908333 -> floor 8174x108  (realized 8192x109; -18px)
  8000x999   rec 0.706707 -> floor 5653x706  (realized 5659x706; -6px)
  50000x100  rec 0.16     -> floor 8000x16   (realized 8192x16; -192px)
  440x240 region, 50k cap rec 0.686364 -> floor 302x164 (realized 302x165)
  8000x1000  symmetric control -> consistent (floor 5656x707 == out)

# Fix in packages/readers-tesseract/src/crop.ts: recorded factor must be
# a 6-decimal-representable value inside
#   [max(outW/cropW,outH/cropH), min((outW+1)/cropW,(outH+1)/cropH))
# — new recordedResizeK() returns ordered candidates (in-interval grid
# nearest midpoint, floor-verified; then below/above interval for
# sub-precision intervals); caller re-derives realized size from the
# accepted factor and re-verifies caps. Same value feeds resizeK and
# the ocr_resize transform record. Edge-cap trigger logic unchanged.

$ bun x --no-install tsc -b tsconfig.json     # post-fix
exit 0

$ node artifacts/tasks/T10/repro-planCrop.mjs   # post-fix
exit 0 — 6/6 PASS; e.g. 4896x6336 records 0.359143,
  floor->1758x2275 exact; 9000x120 records 0.910278 -> 8192x109;
  every case: ocr_resize matrix[0] === resizeK, caps hold.

$ node --test tests/readers/crop-math.mjs
exit 0 — tests 12, pass 12, fail 0 (node:test; no browser/OCR).
  Covers: spec regression (out 1758x2275, resizeK in interval), edge
  caps 9000x120/50000x100, asymmetric 8000x999, region+pad 440x240 @50k,
  sub-precision-interval fallback 8192x4000 @903px (interval
  [0.00524902,0.00525) holds no 1e-6 multiple -> records 0.005249 and
  re-derives realized 42x20, floor-exact), no-downscale k==1, degenerate
  polygon rejection, clipped-padding record, transform-chain shape,
  216 raster x bounds plans + 200 seeded region plans floor-equality
  fuzz (176 + region downscales; all matrix[0]===resizeK, all in caps).

$ bun x --no-install oxlint packages/readers-tesseract/src/crop.ts tests/readers/crop-math.mjs
exit 0 — 0 errors, 2 warnings, both pre-existing at 61dbc3d
  (unused imports mapPoint, OcrError — untouched by this fix).

$ bun x --no-install oxfmt --check <files>
exit 1 — non-conforming under oxfmt defaults, but PRE-EXISTING: the
  committed crop.ts@61dbc3d and untouched errors.ts/reader.ts fail the
  same check. Not reformatted — restyling the package would be
  unrelated churn; new code matches the file's existing style.

# NOT run (pending, heavy/global): bun run test:browser --
# tests/readers/tesseract.spec.ts (expect 11/11), python3
# scripts/task_acceptance.py task T10 [--report run.json],
# bun run verify, bun run build:web.

# ---------------------------------------------------------------------
# Session 3 — devin local SWE-2 subagent (coordinator: Astra)
# Astra's approved arithmetic+render+pixel revision of 32f098e/4282857.
# Authorization update mid-session: T10 now holds the SOLE heavy OCR
# reservation (ZCode's T27 c14e4b0 collected, no-more-heavy queued), so
# the full heavy sequence ran here. Port 5191 is the reservation's
# browser port; the spec harness binds OS-assigned ports per run.
# Toolchain: node v26.7.0, bun 1.4.0, playwright 1.57.0.
# ---------------------------------------------------------------------

$ git fetch origin && git merge origin/main   # coordinator-authorized
exit 0 — clean merge of main 500abfc, 0 conflicts -> merge a5a270f.

$ node -e '<three-candidate arithmetic survey>'
exit 0 — lower-edge candidates reproduce every case:
  4896x6336: r=0.359069 -> 1758x2275 (0.35907 also, 0.359068 -> 1757)
  9000x120: r=0.910223 -> 8192x109   8000x999: r=0.707375 -> 5659x706
  50000x100: r=0.16384 -> 8192x16    8000x1000: r=0.707 -> 5656x707
  440x240@50k: r=0.6875 -> 302x165   8192x4000@903: r=0.00525 -> 43x21
  (previously 42x20 via below-interval fallback — 43x21 is within caps)
  49x80@50px:  r=0.1125  -> 5x9 (fractional dest 5.5125x9)

# Implementation (commit 831e0a1): recordedResizeK returns exactly
# three candidates [m/1e6,(m+1)/1e6,(m-1)/1e6], m=ceil(lo*1e6); caller
# re-derives floor dims per candidate with positive/cap checks, no
# zero-clamping; reader.ts draws the source crop to the FRACTIONAL
# destination crop*resizeK on the integer canvas in both drawImage
# branches, measures the actual right/bottom remainder and reports
# ocr_resize_clipped + CropPlan.resizeClipPx; imageSmoothingEnabled is
# an explicit test seam (default true).

$ bun x --no-install tsc -b tsconfig.json
exit 0

$ node artifacts/tasks/T10/repro-planCrop.mjs   # post-revision
exit 0 — 6/6 PASS; matrix[0]===resizeK everywhere.

$ node --test tests/readers/crop-math.mjs
exit 0 — tests 13, pass 13, fail 0 (12 prior assertions unchanged +
new fractional-destination clipping test).

$ bun x --no-install playwright test tests/readers/crop-pixels.spec.ts
exit 0 — 4/4 pass (1.4s; stub engine only, NO tesseract/worker/model):
  ImageData branch: decoded PNG 5x9, ALL 45 px opaque black (old
    integer-stretch would sample src x≈44.1 -> red at column 4);
    resizeK=0.1125, matrix[0]=matrix[3]=0.1125, resizeClipPx=
    [0.5125,0] (right clip only; 80*0.1125=9 exact); inverse map of
    output center (4.5,4.5) -> canonical (40,40) inside src px [40,41).
  CanvasImageSource branch: identical 5x9 all-black decode.
  No-resize control: k=1, 49x80 out, seam bytes intact (x41 black,
    x42 red), no ocr_resize_clipped limitation.
  Crop-offset region [[34,10]-[41,26]] pad 8 -> crop (26,2) 23x32:
    seam lands at output x=16 (black at 15, red at 16) — nonzero
    source origin restored, no padding leaked, no clip limitation.

$ bun run test:browser -- tests/readers/tesseract.spec.ts   @ 831e0a1+wt
exit 1 — collected=11 passed=10 failed=1. The crop fix itself held:
  'bounded raster/edge/pixel' now passed its line-934 floor assertion
  but hit a LATENT spec bug at line 937 — api.planCropOnly returns a
  Promise (tryV) that was never awaited (edgePlan.ok === undefined);
  unreachable before because the test failed earlier at line 934.
  Harness fix in 567b482 (await both planCropOnly calls).

$ bun run test:browser -- tests/readers/tesseract.spec.ts   @ 567b482
exit 0 — collected=11 passed=11 failed=0 skipped=0 (5.1s). Real
  Tesseract worker/core/model over same-origin staged assets.

$ python3 scripts/task_acceptance.py task T10 \
    --report artifacts/tasks/T10/run.json                     @ 567b482
exit 0 — commands_run=1, failures=[], evidence_errors=[],
  tests {collected 11, passed 11, failed 0, skipped 0};
  evaluated_commit=567b48215ada9d8ed868d4345fdeb7f48dedda7c.

$ bun run verify                                              @ 567b482
exit 0 — group: self-check (16 commands registered) + test:bootstrap
  (49 tests OK) + test:native-bootstrap (2 tests OK) +
  check:coordination (64 tests OK) — 115 tests total.
  [correction 2026-09-12: an earlier draft of this line mislabeled the
  64-test suite as native-bootstrap; the 64 are the coordination tests.
  Attribution corrected here; no result changed — verify exited 0.]

$ bun run build:web                                           @ 567b482
exit 0 — vite build apps/web: 18 modules, dist written (532ms).

# Reservation yielded: no heavy command in flight or queued.
# Pending: independent review + coordinator acceptance receipt
# (receipt.json requires reviewer != worker; not produced locally).

# ============================================================
# WAVE-3 revision (post-Astra rejection of 87e2073, review artifact
# /private/tmp/inkflip-t10-wave2-review.md). All checks below are
# LIGHT: stub-engine browser cases + worker-init-only real-engine
# cases + arithmetic. NO real Tesseract recognize() was run or
# queued — the heavy OCR reservation remains yielded to Astra.
# ============================================================

# Changes vs 87e2073 (reviewed candidate):
#  1) occurrences.ts: word polygon perimeter order corrected to
#     TL,TR,BR,BL (was TL,TR,BL,BR — self-crossing bow-tie, zero
#     shoelace area, rejected by contract validation).
#  2) Verified model bytes are now the engine's actual input:
#     model-cache.ts prepareForEngine() gate (re-commit + read-back
#     verify of the worker-readable idb-keyval slot) runs inside
#     ensureWorker BEFORE createEngineWorker; engine.ts launches the
#     worker with the pinned v7 object payload [{code:'eng',
#     data:'eng'}] + cacheMethod:'readOnly' — the pinned worker's
#     loadLanguage has NO fetch branch for object payloads, so the
#     engine can only consume the verified slot or fail Init honestly.
#     reader.ts constructor requires paths.cachePath===model.cachePath.
#  3) renderReaderId is required config, validated at construction
#     (^[a-z0-9_-]{1,60}$), bound into describe()/plan()/output/
#     occurrence identity, and the produced raster's renderReaderId is
#     re-validated at extraction (mismatch -> render_error).
#  4) Deadline/cancellation: the adjudicated pdf-ebz boundary — the
#     T02-approved WorkerOptions.signal patch is consumed here.
#     OpScope = {op seq, absolute deadlineAt, cancellation, handle};
#     withDeadline races every external await (raster source, blob
#     encode, arrayBuffer, prepare, init, recognize) against the ONE
#     absolute deadline + cancel poll; guard() after every await and
#     before install/progress/emit/accounting; a retry draws only the
#     remaining budget; close() invalidates (closed + opSeq++ +
#     handle=null) BEFORE teardown; workerInitCtl lets destroyWorker
#     abort an in-flight init's raw worker (found + fixed during the
#     light lifecycle tests — previously the controller was only
#     reachable after install); joined inits never overlap; late
#     init resolution self-terminates; progress is dropped unless the
#     emitting op is current (jobOps for recognize-scoped job ids).
#  5) Raw-text run budget counts UTF-8 bytes (TextEncoder), not
#     UTF-16 code units.
#  6) imageSmoothingEnabled public option removed entirely (config,
#     cropToBlob, call sites, pixel tests) — browser default stays.
#  7) The four crop-pixel regressions moved INTO the registered spec
#     (tests/readers/tesseract.spec.ts 'light:' describes, stub engine
#     at the decoded-PNG boundary); shared stub layer is the test-only
#     tests/readers/t10-pixel-harness.ts (PIXEL_ENTRY_SNIPPET is
#     appended to the same bundle — one server, no separate harness,
#     no top-level hooks). crop-pixels.spec.ts deleted.
#  8) Harness: tesseract.js is now bundled from the pinned package
#     ENTRY src/index.js (not dist/tesseract.esm.min.js) so tests
#     exercise the patched WorkerOptions.signal path.

$ bun x --no-install tsc -b tsconfig.json
exit 0

$ node --test tests/readers/crop-math.mjs
exit 0 — tests 13, pass 13, fail 0.

$ bun x --no-install oxlint packages/readers-tesseract/src/ \
    tests/readers/tesseract.spec.ts tests/readers/t10-pixel-harness.ts
exit 0 — 0 warnings, 0 errors (unused imports Region/
LOW_CONFIDENCE_BELOW/mapPoint/OcrError removed; useless spread
fallback simplified).

$ bun x --no-install playwright test tests/readers/tesseract.spec.ts \
    -g 'light:' --reporter=line
exit 0 — 24/24 pass (~6s). LIGHT only: stub engine at the decoded-PNG
  boundary + real-engine worker INIT checks (no recognize on a real
  model anywhere in this selection):
  - crop pixel regressions x4 (ImageData + CanvasImageSource branches,
    no-resize control, crop-offset region) — same assertions as the
    deleted crop-pixels.spec.ts, now inside the registered file.
  - renderer identity x3 (plan/extract/describe agree; foreign raster
    -> render_error; malformed/missing config ids -> unsupported).
  - occurrence geometry + report x2 (polygon [[2,2],[20,2],[20,12],
    [2,12]] TL,TR,BR,BL, |signed area|=180; assembled Report passes
    contracts.validateReport with strict=false).
  - UTF-8 raw-text budget x1 ('é' rejected at 1-byte cap, accepted at
    2; '😀' rejected at 3-byte cap).
  - verified model bytes x5 (stub captures langs [{code:'eng',
    data:'eng'}] + cacheMethod 'readOnly' + signal AbortSignal +
    Uint8Array PNG input; real-engine init on a pre-corrupted slot
    succeeds after delete+refetch+verify+commit; divergent-second-
    response route proves the engine fetched nothing itself
    (served==1); idbFactory 'none' and 'writeFail' both fail open()
    with missing_model BEFORE any createWorker).
  - deadline/cancellation lifecycle x9 (stalled raster -> timeout at
    ~1x deadline + worker terminated; cancel during raster -> fast
    user_cancel; hung extract-time init -> timeout, no overlap;
    cancel during init -> user_cancel + typed abort reason reached the
    engine's signal; late init success self-terminates and installs
    nothing; flakyHang retry draws the SHARED deadline ~1x not 2x;
    hung recognize -> timeout + terminate; cancel-before-extract never
    touches the engine; close() during REAL open aborts the in-flight
    raw worker — openRes 'worker_crash', workerInitCount 0,
    page.workers() back to baseline).

$ bun x --no-install playwright test tests/readers/tesseract.spec.ts \
    --list
exit 0 — 35 tests collected in the registered file: 11 real-OCR +
  24 light. Registered command unchanged:
  bun run test:browser -- tests/readers/tesseract.spec.ts

# Heavy status: real Tesseract recognize() was NOT run on this
# revision — the 11 real-OCR tests above the 'light:' block are
# unchanged in intent but the reader implementation changed beneath
# them (verified-byte engine input, deadline/cancellation, renderer
# identity, utf-8 budget). The heavy reservation remains yielded to
# Astra; nothing heavy is running or queued.

# ============================================================
# WAVE-3 HEAVY RUN — sole reservation holder (grant on port 5191;
# pdf-ebz merge precondition satisfied: c1f82bd merged as e6ab3b5;
# harness bundles the patched tesseract.js src/index.js entry).
# ============================================================

$ bun run test:browser -- tests/readers/tesseract.spec.ts   [attempt 1]
exit 1 — 33 passed / 2 failed (10.7s), both real-OCR legs:
  'worker init failure ... never unreadable_pixels': blank run ->
    check.status 'failed', reason 'render_error'.
  'word boxes map back ...': noise run -> 'failed'/'render_error'.
  CLASSIFICATION: test-harness defect, not product. The wave-3
  extract-time re-validation correctly rejects rasters whose
  renderReaderId differs from the reader's configured renderer —
  the harness labels blank/noise rasters 'synthetic-harness' while
  the consuming readers were configured 'pdfjs-6_3_289-render'.
  FIX (fa59348, no assertion weakened): test 825 binds the healthy
  reader to 'synthetic-harness'; test 1147 runs the noise leg on a
  second reader bound to 'synthetic-harness' (same page slot) —
  provenance stays honest; a pdfjs-bound reader never re-identifies.

$ bun x --no-install playwright test tests/readers/tesseract.spec.ts \
    -g 'worker init failure is a terminal|word boxes map back'
exit 0 — 2/2 pass (2.7s) after the binding fix.

$ bun run test:browser -- tests/readers/tesseract.spec.ts   [attempt 2]
exit 0 — 35/35 pass (10.1s): 11 real-OCR + 24 light, one worker.
  Real-OCR leg exercised on this revision: real patched worker init,
  verified model payload (object-language + readOnly cache slot),
  real recognize() over prepared Uint8Array PNG, worker reuse,
  retry on fresh worker, cancel->terminate, cold/memory/cached model
  states, bounded pixel/edge caps, diagnostic confidence.

$ python3 scripts/task_acceptance.py task T10 \
    --report artifacts/tasks/T10/run.json
exit 0 — evaluated_commit fa5934824361045520eeb32c2a76ff37b3ae9d13;
  commands_run 1; failures []; tests 35 collected / 35 passed /
  0 failed / 0 skipped.
  (First invocation refused: "commit implementation changes before
  recording acceptance evidence" — test fix committed as fa59348.)

$ bun run verify
exit 0 — 115 tests total (49 bootstrap/acceptance + 2 native-boundary
  + 64 coordination/homebase), 1 pre-existing skip inside the
  skip-detection meta-test; self-check: 16 commands.

$ bun run build:web
exit 0 — vite 8.3.0, 18 modules, dist/assets/index-DLl647JV.js
  191.77 kB gzip 60.54 kB, built in 205ms.

# RELEASE: no heavy command in flight or queued after this block.


# ============================================================
# WAVE-4 REVISION — model-provenance + operation-scoped lifetime
# repair (Astra wave-4 direction; Pauli wave-3 review findings).
# Heavy reservation belongs to Homebase ZCode (T27) — LIGHT
# CHECKS ONLY in this block: source/typecheck/math/stub/pixel.
# Real-engine registered cases are present but NOT run here
# (excluded under the hold; see excluded list below).
# ============================================================

$ git merge origin/main
exit 0 — merge commit 744f527; main 1f0d900 carries the reviewed
  pdf-q38 language-payload repair (14f062e): patched
  initializeInternal normalizes object Lang payloads to their
  .code for Init(), while loadLanguage consumes the object .data
  bytes. Verified installed: src/createWorker.js:149-153,
  worker-script/index.js:102,153-155. Frozen install unchanged.

# Implementation (uncommitted in this block):
#  engine.ts — worker gets [{code:lang,data:verifiedBytes.slice()}]
#    + cacheMethod 'none'; the adapter-verified snapshot IS the
#    engine input (worker writes it verbatim to /eng.traineddata;
#    no fetch/cache branch for object payloads). prepareForEngine,
#    mandatory read-back and cachePath-equality coupling removed.
#  model-cache.ts — prepare(signal): epoch + signal checked after
#    every internal await and before every state/memory/cache
#    mutation; a superseded/removed preparation can neither
#    publish states nor recreate a deleted slot (post-write
#    invalidate -> best-effort delete of our own write). remove()
#    bumps epoch first. IDB unavailable/write-fail -> truthful
#    memory-only success with model_cache_unavailable limitation.
#  reader.ts — OpScope gains an OWNED terminal AbortController
#    (close/supersession/caller-cancel fire it; deadline stays
#    timeout); liveScopes + jobScopes admission for progress AND
#    error callbacks; WorkerLease {owner, ctl, init, joiners,
#    settled, worker} — the settle hook is the ONLY installer
#    (install into live unaborted slot OR self-terminate orphan);
#    op-scoped cleanup everywhere (stale open/extract can never
#    clear a newer handle, kill a shared/installed worker, or
#    zero live accounting); extract rejects foreign handles
#    before scoping.

$ bun x --no-install tsc -b tsconfig.json
exit 0

$ bun x --no-install oxlint packages/readers-tesseract/src/ \
    tests/readers/tesseract.spec.ts tests/readers/t10-pixel-harness.ts
exit 0 — 0 warnings, 0 errors (after removing a backtick inside a
  template-literal comment, an unused import, and a useless spread).

$ node --test tests/readers/crop-math.mjs
exit 0 — 13/13 pass.

$ node artifacts/tasks/T10/repro-planCrop.mjs
exit 0 — ALL OK (6/6 candidates, matrix[0]===resizeK everywhere).

$ bun run test:browser -- tests/readers/tesseract.spec.ts --workers=1 \
    -g 'light:' --grep-invert \
    'a stale or corrupt cache slot|the engine consumes the verified slot|close during a real open'
  [attempt 1]
exit 1 — 21 passed / 4 failed: stale-settle-hook race terminated
  every produced worker before its awaiter could install
  (.then handlers run before the awaiting continuation):
  'a stalled raster resolves to timeout' terminateCount 2!=1,
  'a late init success' 3!=2, 'a hung recognize' 3!=2,
  'superseded open' terminated [true]!=[false].
  CLASSIFICATION: product defect in the new lease code — fixed by
  making the settle hook the ONLY installer (install iff lease is
  current+unaborted+unfilled, else orphan-terminate); awaiters
  validate, never install. No assertion weakened.

$ bun run test:browser -- tests/readers/tesseract.spec.ts --workers=1 \
    -g 'a stalled raster resolves|a late init success|a hung recognize|superseded open'
exit 0 — 4/4 pass (2.2s).

$ bun run test:browser -- tests/readers/tesseract.spec.ts --workers=1 \
    -g 'light:' --grep-invert \
    'a stale or corrupt cache slot|the engine consumes the verified slot|close during a real open'
  [attempt 2]
exit 0 — 25/25 pass (~5.5s), including:
  - 4 ported independent stub lifecycle probes (close settles a
    stalled raster fast w/ user_cancel; late model prep cannot
    publish or recreate deleted data after close+remove; superseded
    open cannot clear the newer handle/worker; late init progress
    AND error callbacks dropped after timeout w/ signal aborted).
  - worker init payload: [{code:'eng',data:Uint8Array snapshot}]
    whose byteLength+sha256 equal the prepared model exactly,
    cacheMethod 'none', AbortSignal present, PNG bytes input.
  - idbFactory none / writeFail -> truthful ready_memory success
    with model_cache_unavailable limitation + verified payload.

# EXCLUDED UNDER THE HOLD (registered but not run — real engine):
#   'a stale or corrupt cache slot is replaced before the engine
#    sees it' (real worker init)
#   'the engine consumes the verified slot — never a second
#    divergent copy' (real worker init + route counting)
#   'close during a real open aborts the raw worker init'
#   'independent real cache provenance' describe — the new Pauli
#    pair: unchanged-cache control + cache-replacement-after-
#    verification (asserts engine FS sha == reported sha, exactly
#    one model request). Registered, awaits the slot transfer.
#
# Registered collection now: 41 tests (11 real-OCR + 24 prior light
# reworked + 4 stub lifecycle probes + 2 real provenance probes).
# No heavy command in flight or queued; reservation NOT held.
