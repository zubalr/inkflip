Reconstructed upstream digest matches: src/createWorker.js
Reconstructed upstream digest matches: src/index.d.ts
Earlier public types unchanged byte-for-byte
--- base/src/createWorker.js
+++ candidate/src/createWorker.js
@@ -146,7 +146,14 @@
     startJob(createJob({
       id: jobId,
       action: 'initialize',
-      payload: { langs: _langs, oem: _oem, config: _config },
+      // The worker's object branch uses .data as the Init language name.
+      // Keep model bytes in loadLanguage, and send only codes to initialize.
+      payload: {
+        langs: typeof _langs === 'string' ? _langs
+          : _langs.map((lang) => (typeof lang === 'string' ? lang : lang.code)).join('+'),
+        oem: _oem,
+        config: _config,
+      },
     }))
   );
 
