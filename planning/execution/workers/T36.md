# T36 — Build independent evaluation protocol and held-out corpus

## Copy-ready worker prompt

You own **T36**, role **evaluation-custodian**. Use the assigned isolated checkout. The task is planned, not implemented. Verify dependencies **T05, T03** are accepted with fresh merged-branch evidence before editing. Read the task's canonical record in `planning/execution/tasks.json`; this brief is its generated view.

Read these compact inputs from `planning/`: `PROJECT_BRIEF.md`, `architecture/GLOSSARY_AND_INVARIANTS.md`, `DECISIONS.md`, `quality/EVALUATION.md`, `quality/FIXTURE_PROGRAM.md`, `research/EXPERIMENT_MATRIX.md`.

Current-code anchors: Legacy137.89/150 and all rival reported gains are not inspector metrics

Freeze grouped development/public/evaluation splits, generator families and truth labels under independent custodian control. Implement precise denominators, confidence intervals, clean/damaged metrics and diagnosis test forms. Tuning workers do not get untouched labels.

## Deliver and own

- `evaluation/protocol/`
- `evaluation/manifests/`
- `scripts/evaluate_inspector.py`
- `tests/evaluation/`

These are target-repository paths, not files claimed to exist in this planning ZIP. Evidence-only receipt/proposal namespaces are allowed under [ownership](../OWNERSHIP.md). Shared contracts, dependencies, root composition and state acceptance require their named owner's lease. Submit state changes to the coordinator; do not accept your own task.

## Contract and exclusions

Canonical contract **1.0.0**. Required invariants: I06, I12, I16, I18.

No product-category expansion, unbounded inference, automatic correction or undocumented fallback. Research candidates do not enter production in this task unless explicitly assigned and reviewed.

Forbidden: Original mib-intake remote/checkout; Other tasks’ owned modules without approved change proposal; Unapproved shared schemas/lockfiles/routing/goldens; Public uploads, SaaS/auth/billing, unapproved external publication; Reserved evaluation labels for optimization workers.

## Execute acceptance

Fixture families: F01, F03, F04, F11, F12, F14, F15, F23. Test records: TEST-36. Required assertions:

- No sibling leakage.
- skipped/failed pages remain coverage denominator.
- repeated OCR readings are not independent samples.
- targets reported as unmet until real run.
- small real incident data stays permissioned and out of public logs.

```sh
python -m unittest discover -s tests/evaluation -v
```

These are implementation acceptance commands, not a claim of execution in planning. Gate commands follow the explicit nonrecursive scenario map. A missing dependency, test, required device or tool is blocked evidence, not success. Record the exact implementation and merged commits, versions, command exits, collected/passed/failed/skipped counts, fixture/model hashes, raw outputs and limitations in `artifacts/tasks/T36/receipt.json`, `artifacts/tasks/T36/commands.log`, `artifacts/tasks/T36/review.md`.

## Failure and rollback

Classify failure and preserve completed evidence. Do not relax contract, skip a required profile, fabricate a result or update a baseline to hide it. Report exact blocked capability and continue unrelated ready work.

Revert this task’s commits on the integration branch; invalidate dependent evidence, preserve experiment receipts and originals. No destructive cleanup outside assigned worktree.

## Plan mismatch and handoff

Create docs/proposals/T36.md with observed source, minimal reproducer, affected contracts/tasks/tests and recommended correction. Pause only affected work. Contract owner and integrator decide, update canonical records, then rebase consumers.

Submit the T36 state transition and evidence paths to the coordinator; only the state owner changes execution/state.json. Module documentation and executable command examples. docs/ATTRIBUTION.md for material reuse/inspiration. Experiment receipt for any measured/rejected alternative. Independent review and merged-branch acceptance receipt.

Request [independent review](../reviews/T36.md). The implementer cannot approve their own material change. The integrator reruns acceptance on the merged branch, records that commit and only then changes state to accepted. An optional experiment may be rejected only with complete baseline, clean-control, result and review evidence.
