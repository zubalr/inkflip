# T54 — Owner-authorized static deployment, rollback and final release

## Copy-ready worker prompt

You own **T54**, role **release-owner**. Use the assigned isolated checkout. The task is planned, not implemented. Verify dependencies **T55** are accepted with fresh merged-branch evidence before editing. Read the task's canonical record in `planning/execution/tasks.json`; this brief is its generated view.

Read these compact inputs from `planning/`: `PROJECT_BRIEF.md`, `architecture/GLOSSARY_AND_INVARIANTS.md`, `DECISIONS.md`, `deployment/RELEASE_RUNBOOK.md`, `deployment/STATIC_DEPLOYMENT.md`, `execution/RELEASE_CHECKLIST.md`, `OWNER_INPUTS.md`.

Current-code anchors: Account identifiers, domain and deployment secrets must be supplied securely by the owner. This planning pass performs no deployment.

After explicit owner approval, deploy exact static artifact to supplied account/domain, test real request paths/headers/no-egress/account-level script injection, practice rollback to an exact archived artifact, tag release and publish only approved source and media. No unrelated paid service is enabled.

## Deliver and own

- `artifacts/gates/G5/`
- `artifacts/releases/`
- `docs/releases/`
- `tests/deployment/public.spec.ts`

These are target-repository paths, not files claimed to exist in this planning ZIP. Evidence-only receipt/proposal namespaces are allowed under [ownership](../OWNERSHIP.md). Shared contracts, dependencies, root composition and state acceptance require their named owner's lease. Submit state changes to the coordinator; do not accept your own task.

## Contract and exclusions

Canonical contract **1.0.0**. Required invariants: I08, I13, I15, I16, I18.

No product-category expansion, unbounded inference, automatic correction or undocumented fallback. Research candidates do not enter production in this task unless explicitly assigned and reviewed.

Forbidden: Original mib-intake remote/checkout; Other tasks’ owned modules without approved change proposal; Unapproved shared schemas/lockfiles/routing/goldens; Public uploads, SaaS/auth/billing, unapproved external publication; Reserved evaluation labels for optimization workers.

## Execute acceptance

Fixture families: F01, F03, F20, F22, F24. Test records: TEST-54. Required assertions:

- The public sample and own-file workflow work on the actual authorized origin.
- Deployment configuration, request-path tests and available account metrics establish that application traffic does not invoke a dynamic Worker.
- No upload endpoint or account-injected analytics script is present.
- Rollback serves the previously archived artifact and its verified hashes.
- The archived distribution, model manifest, SBOM, source, test receipts and public claims match the release.
- The owner records explicit publication approval without disclosing secrets.

```sh
python scripts/gate.py G5 --target "$INKFLIP_PUBLIC_ORIGIN"
```

These are implementation acceptance commands, not a claim of execution in planning. Gate commands follow the explicit nonrecursive scenario map. A missing dependency, test, required device or tool is blocked evidence, not success. Record the exact implementation and merged commits, versions, command exits, collected/passed/failed/skipped counts, fixture/model hashes, raw outputs and limitations in `artifacts/tasks/T54/receipt.json`, `artifacts/tasks/T54/commands.log`, `artifacts/tasks/T54/review.md`.

## Failure and rollback

Classify failure and preserve completed evidence. Do not relax contract, skip a required profile, fabricate a result or update a baseline to hide it. Report exact blocked capability and continue unrelated ready work.

Revert this task’s commits on the integration branch; invalidate dependent evidence, preserve experiment receipts and originals. No destructive cleanup outside assigned worktree.

## Plan mismatch and handoff

Create docs/proposals/T54.md with observed source, minimal reproducer, affected contracts/tasks/tests and recommended correction. Pause only affected work. Contract owner and integrator decide, update canonical records, then rebase consumers.

Submit the T54 state transition and evidence paths to the coordinator; only the state owner changes execution/state.json. Module documentation and executable command examples. docs/ATTRIBUTION.md for material reuse/inspiration. Experiment receipt for any measured/rejected alternative. Independent review and merged-branch acceptance receipt.

Request [independent review](../reviews/T54.md). The implementer cannot approve their own material change. The integrator reruns acceptance on the merged branch, records that commit and only then changes state to accepted. An optional experiment may be rejected only with complete baseline, clean-control, result and review evidence.
