# T02 — Freeze actual dependency locks, model assets and build provenance

## Copy-ready worker prompt

You own **T02**, role **supply-chain-owner**. Use the assigned isolated checkout. The task is planned, not implemented. Verify dependencies **T01** are accepted with fresh merged-branch evidence before editing. Read the task's canonical record in `planning/execution/tasks.json`; this brief is its generated view.

Read these compact inputs from `planning/`: `PROJECT_BRIEF.md`, `architecture/GLOSSARY_AND_INVARIANTS.md`, `DECISIONS.md`, `config/dependencies.json`, `config/model-assets.json`, `security/LICENSE_AND_ATTRIBUTION.md`, `deployment/STATIC_DEPLOYMENT.md`.

Current-code anchors: Selected official versions in research/SOURCES.md S22–S39; native installed versions are probe baselines only

Install selected compatible packages in isolated environments with owner-authorized setup network; verify engines/advisories, exact package/model hashes and notices. Pin compatible patched toolchain builds and full action SHAs; stage all browser resources same-origin. Add exact pytest, browser accessibility and test-only dependencies to the lock with purpose.

## Deliver and own

- `pnpm-lock.yaml`
- `native/uv.lock`
- `.node-version`
- `.python-version`
- `config/resolved-assets.json`
- `scripts/prepare_assets.py`
- `scripts/check_dependencies.py`
- `build/base-image.lock.json`
- `package.json`
- `native/pyproject.toml`
- `apps/web/package.json`
- `config/test-toolchain.json`

These are target-repository paths, not files claimed to exist in this planning ZIP. Evidence-only receipt/proposal namespaces are allowed under [ownership](../OWNERSHIP.md). Shared contracts, dependencies, root composition and state acceptance require their named owner's lease. Submit state changes to the coordinator; do not accept your own task.

## Contract and exclusions

Canonical contract **1.0.0**. Required invariants: I13, I16.

No product-category expansion, unbounded inference, automatic correction or undocumented fallback. Research candidates do not enter production in this task unless explicitly assigned and reviewed.

Forbidden: Original mib-intake remote/checkout; Other tasks’ owned modules without approved change proposal; Unapproved shared schemas/lockfiles/routing/goldens; Public uploads, SaaS/auth/billing, unapproved external publication; Reserved evaluation labels for optimization workers.

## Execute acceptance

Fixture families: F20, F22. Test records: TEST-02. Required assertions:

- Frozen install succeeds twice from a clean checkout.
- every bundled binary has source/hash/license.
- substituted model fails checksum.
- missing asset fails closed.
- no runtime CDN/download.
- action/OCI revisions are immutable and recorded.

```sh
python scripts/check_dependencies.py --frozen && python -m unittest discover -s tests/build -v
```

These are implementation acceptance commands, not a claim of execution in planning. Gate commands follow the explicit nonrecursive scenario map. A missing dependency, test, required device or tool is blocked evidence, not success. Record the exact implementation and merged commits, versions, command exits, collected/passed/failed/skipped counts, fixture/model hashes, raw outputs and limitations in `artifacts/tasks/T02/receipt.json`, `artifacts/tasks/T02/commands.log`, `artifacts/tasks/T02/review.md`.

## Failure and rollback

Classify failure and preserve completed evidence. Do not relax contract, skip a required profile, fabricate a result or update a baseline to hide it. Report exact blocked capability and continue unrelated ready work.

Revert this task’s commits on the integration branch; invalidate dependent evidence, preserve experiment receipts and originals. No destructive cleanup outside assigned worktree.

## Plan mismatch and handoff

Create docs/proposals/T02.md with observed source, minimal reproducer, affected contracts/tasks/tests and recommended correction. Pause only affected work. Contract owner and integrator decide, update canonical records, then rebase consumers.

Submit the T02 state transition and evidence paths to the coordinator; only the state owner changes execution/state.json. Module documentation and executable command examples. docs/ATTRIBUTION.md for material reuse/inspiration. Experiment receipt for any measured/rejected alternative. Independent review and merged-branch acceptance receipt.

Request [independent review](../reviews/T02.md). The implementer cannot approve their own material change. The integrator reruns acceptance on the merged branch, records that commit and only then changes state to accepted. An optional experiment may be rejected only with complete baseline, clean-control, result and review evidence.
