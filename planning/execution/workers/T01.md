# T01 — Bootstrap the separate monorepo and honest origin

## Copy-ready worker prompt

You own **T01**, role **integrator**. Use the assigned isolated checkout. The task is planned, not implemented. Verify dependencies **none** are accepted with fresh merged-branch evidence before editing. Read the task's canonical record in `planning/execution/tasks.json`; this brief is its generated view.

Read these compact inputs from `planning/`: `PROJECT_BRIEF.md`, `architecture/GLOSSARY_AND_INVARIANTS.md`, `DECISIONS.md`, `architecture/REPOSITORY_AND_DEPENDENCIES.md`, `architecture/ORIGIN_AND_MIGRATION.md`, `deployment/CI_AND_LOCAL_COMMANDS.md`.

Current-code anchors: mib-intake README, LICENSE and Dockerfile at 94f35ce9f9beb1640ddebdc2c72aa379ecebb004

Create local package boundaries, the nonempty-test command harness, copy planning inputs, and document snapshot provenance without copying legacy adjudication. Initialize early read-only CI; no remote, deployment or dependency install is implicit. Create the minimal Vite/React entry and module build boundaries needed by T02/T06/T13. Keep the scaffold explicitly unfinished and local; do not publish empty or simulated product controls. Retain the exact original license at the documented origin path.

## Deliver and own

- `package.json`
- `pnpm-workspace.yaml`
- `native/pyproject.toml`
- `scripts/task_acceptance.py`
- `scripts/gate.py`
- `.github/workflows/ci.yml`
- `docs/ORIGIN.md`
- `apps/web/package.json`
- `apps/web/index.html`
- `apps/web/vite.config.ts`
- `apps/web/tsconfig.json`
- `apps/web/src/main.tsx`
- `apps/web/src/App.tsx`
- `third_party/origin/mib-intake/LICENSE`

These are target-repository paths, not files claimed to exist in this planning ZIP. Evidence-only receipt/proposal namespaces are allowed under [ownership](../OWNERSHIP.md). Shared contracts, dependencies, root composition and state acceptance require their named owner's lease. Submit state changes to the coordinator; do not accept your own task.

## Contract and exclusions

Canonical contract **1.0.0**. Required invariants: I01, I16.

No product-category expansion, unbounded inference, automatic correction or undocumented fallback. Research candidates do not enter production in this task unless explicitly assigned and reviewed.

Forbidden: Original mib-intake remote/checkout; Other tasks’ owned modules without approved change proposal; Unapproved shared schemas/lockfiles/routing/goldens; Public uploads, SaaS/auth/billing, unapproved external publication; Reserved evaluation labels for optimization workers.

## Execute acceptance

Fixture families: F01. Test records: TEST-01. Required assertions:

- A clean checkout exposes documented commands.
- empty/missing test registration fails.
- old repository remains byte/Git-unchanged.
- no network action or generated achievement claim occurs.

```sh
python -m unittest discover -s tests/bootstrap -v
```

These are implementation acceptance commands, not a claim of execution in planning. Gate commands follow the explicit nonrecursive scenario map. A missing dependency, test, required device or tool is blocked evidence, not success. Record the exact implementation and merged commits, versions, command exits, collected/passed/failed/skipped counts, fixture/model hashes, raw outputs and limitations in `artifacts/tasks/T01/receipt.json`, `artifacts/tasks/T01/commands.log`, `artifacts/tasks/T01/review.md`.

## Failure and rollback

Classify failure and preserve completed evidence. Do not relax contract, skip a required profile, fabricate a result or update a baseline to hide it. Report exact blocked capability and continue unrelated ready work.

Revert this task’s commits on the integration branch; invalidate dependent evidence, preserve experiment receipts and originals. No destructive cleanup outside assigned worktree.

## Plan mismatch and handoff

Create docs/proposals/T01.md with observed source, minimal reproducer, affected contracts/tasks/tests and recommended correction. Pause only affected work. Contract owner and integrator decide, update canonical records, then rebase consumers.

Submit the T01 state transition and evidence paths to the coordinator; only the state owner changes execution/state.json. Module documentation and executable command examples. docs/ATTRIBUTION.md for material reuse/inspiration. Experiment receipt for any measured/rejected alternative. Independent review and merged-branch acceptance receipt.

Request [independent review](../reviews/T01.md). The implementer cannot approve their own material change. The integrator reruns acceptance on the merged branch, records that commit and only then changes state to accepted. An optional experiment may be rejected only with complete baseline, clean-control, result and review evidence.
