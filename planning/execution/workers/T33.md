# T33 — Implement explicit version-isolated reader profiles

## Copy-ready worker prompt

You own **T33**, role **native-environments**. Use the assigned isolated checkout. The task is planned, not implemented. Verify dependencies **T02, T30** are accepted with fresh merged-branch evidence before editing. Read the task's canonical record in `planning/execution/tasks.json`; this brief is its generated view.

Read these compact inputs from `planning/`: `PROJECT_BRIEF.md`, `architecture/GLOSSARY_AND_INVARIANTS.md`, `DECISIONS.md`, `architecture/CLI_AND_REGRESSION.md`, `security/THREAT_MODEL.md`, `deployment/examples/reader-upgrade.sh`.

Current-code anchors: Pinned pypdf baseline5.9.0 and selected6.18.0 example; no same-process dual-version assumption

Implement explicitly invoked local profile setup for incompatible versions in separate virtual environments, trusted allowlisted reader wrappers and recorded executable/environment identity. Setup can download only on operator command, never on report import/replay. Also provide the fixed Node PDF.js profile wrapper inside packages/readers-pdfjs/node; use the shared PDF.js adapter and separate Node workspace locks. This is a named-reader profile, not arbitrary executable discovery.

## Deliver and own

- `scripts/install_reader_profile.py`
- `native/inkflip/profiles/`
- `native/tests/profiles/`
- `packages/readers-pdfjs/node/`

These are target-repository paths, not files claimed to exist in this planning ZIP. Evidence-only receipt/proposal namespaces are allowed under [ownership](../OWNERSHIP.md). Shared contracts, dependencies, root composition and state acceptance require their named owner's lease. Submit state changes to the coordinator; do not accept your own task.

## Contract and exclusions

Canonical contract **1.0.0**. Required invariants: I10, I13, I17.

No product-category expansion, unbounded inference, automatic correction or undocumented fallback. Research candidates do not enter production in this task unless explicitly assigned and reviewed.

Forbidden: Original mib-intake remote/checkout; Other tasks’ owned modules without approved change proposal; Unapproved shared schemas/lockfiles/routing/goldens; Public uploads, SaaS/auth/billing, unapproved external publication; Reserved evaluation labels for optimization workers.

## Execute acceptance

Fixture families: F23, F24, F25. Test records: TEST-33. Required assertions:

- Old/new pypdf versions demonstrably loaded from distinct interpreters.
- reader version printed in outputs.
- missing version install blocks that profile.
- untrusted profile path/command refused.
- offline run makes no requests.

```sh
uv run --project native python -m pytest native/tests/profiles -q
```

These are implementation acceptance commands, not a claim of execution in planning. Gate commands follow the explicit nonrecursive scenario map. A missing dependency, test, required device or tool is blocked evidence, not success. Record the exact implementation and merged commits, versions, command exits, collected/passed/failed/skipped counts, fixture/model hashes, raw outputs and limitations in `artifacts/tasks/T33/receipt.json`, `artifacts/tasks/T33/commands.log`, `artifacts/tasks/T33/review.md`.

## Failure and rollback

Classify failure and preserve completed evidence. Do not relax contract, skip a required profile, fabricate a result or update a baseline to hide it. Report exact blocked capability and continue unrelated ready work.

Revert this task’s commits on the integration branch; invalidate dependent evidence, preserve experiment receipts and originals. No destructive cleanup outside assigned worktree.

## Plan mismatch and handoff

Create docs/proposals/T33.md with observed source, minimal reproducer, affected contracts/tasks/tests and recommended correction. Pause only affected work. Contract owner and integrator decide, update canonical records, then rebase consumers.

Submit the T33 state transition and evidence paths to the coordinator; only the state owner changes execution/state.json. Module documentation and executable command examples. docs/ATTRIBUTION.md for material reuse/inspiration. Experiment receipt for any measured/rejected alternative. Independent review and merged-branch acceptance receipt.

Request [independent review](../reviews/T33.md). The implementer cannot approve their own material change. The integrator reruns acceptance on the merged branch, records that commit and only then changes state to accepted. An optional experiment may be rejected only with complete baseline, clean-control, result and review evidence.
