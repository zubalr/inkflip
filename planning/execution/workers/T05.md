# T05 — Create original fixture foundation and rights manifest

## Copy-ready worker prompt

You own **T05**, role **fixture-author**. Use the assigned isolated checkout. The task is planned, not implemented. Verify dependencies **T01** are accepted with fresh merged-branch evidence before editing. Read the task's canonical record in `planning/execution/tasks.json`; this brief is its generated view.

Read these compact inputs from `planning/`: `PROJECT_BRIEF.md`, `architecture/GLOSSARY_AND_INVARIANTS.md`, `DECISIONS.md`, `quality/FIXTURE_PROGRAM.md`, `quality/fixture-catalog.json`, `product/DEMO_AND_GALLERY.md`, `probes/make_fixtures.py`.

Current-code anchors: Own tests/test_pdfio.py informs mechanism controls; generated recipes in this package are original

Port the original fixed harmless generator and develop controlled public/development families; retain exact generator intent, expected raw reader outputs and clean twins. Keep evaluation labels outside optimization checkout. Include the legitimate searchable-scan F03 minimum control before G1; the full gallery presentation remains T21.

## Deliver and own

- `fixtures/public/`
- `fixtures/development/`
- `scripts/make_fixtures.py`
- `fixtures/manifest.json`
- `tests/fixtures/`

These are target-repository paths, not files claimed to exist in this planning ZIP. Evidence-only receipt/proposal namespaces are allowed under [ownership](../OWNERSHIP.md). Shared contracts, dependencies, root composition and state acceptance require their named owner's lease. Submit state changes to the coordinator; do not accept your own task.

## Contract and exclusions

Canonical contract **1.0.0**. Required invariants: I01, I13, I16.

No product-category expansion, unbounded inference, automatic correction or undocumented fallback. Research candidates do not enter production in this task unless explicitly assigned and reviewed.

Forbidden: Original mib-intake remote/checkout; Other tasks’ owned modules without approved change proposal; Unapproved shared schemas/lockfiles/routing/goldens; Public uploads, SaaS/auth/billing, unapproved external publication; Reserved evaluation labels for optimization workers.

## Execute acceptance

Fixture families: F01, F02, F07, F08, F03. Test records: TEST-05. Required assertions:

- Repeated generation is byte-identical or declared pinned asset-dependent.
- mapping/control render equal under same environment and actual extraction differs.
- no filename-conditioned app output.
- no embedded unlicensed font or private/challenge answer data.
- An original legitimate searchable scan (F03) is generated and read without an alarm solely because its OCR text is invisible; this minimum control is present before G1.

```sh
python scripts/make_fixtures.py --check && python -m unittest discover -s tests/fixtures -v
```

These are implementation acceptance commands, not a claim of execution in planning. Gate commands follow the explicit nonrecursive scenario map. A missing dependency, test, required device or tool is blocked evidence, not success. Record the exact implementation and merged commits, versions, command exits, collected/passed/failed/skipped counts, fixture/model hashes, raw outputs and limitations in `artifacts/tasks/T05/receipt.json`, `artifacts/tasks/T05/commands.log`, `artifacts/tasks/T05/review.md`.

## Failure and rollback

Classify failure and preserve completed evidence. Do not relax contract, skip a required profile, fabricate a result or update a baseline to hide it. Report exact blocked capability and continue unrelated ready work.

Revert this task’s commits on the integration branch; invalidate dependent evidence, preserve experiment receipts and originals. No destructive cleanup outside assigned worktree.

## Plan mismatch and handoff

Create docs/proposals/T05.md with observed source, minimal reproducer, affected contracts/tasks/tests and recommended correction. Pause only affected work. Contract owner and integrator decide, update canonical records, then rebase consumers.

Submit the T05 state transition and evidence paths to the coordinator; only the state owner changes execution/state.json. Module documentation and executable command examples. docs/ATTRIBUTION.md for material reuse/inspiration. Experiment receipt for any measured/rejected alternative. Independent review and merged-branch acceptance receipt.

Request [independent review](../reviews/T05.md). The implementer cannot approve their own material change. The integrator reruns acceptance on the merged branch, records that commit and only then changes state to accepted. An optional experiment may be rejected only with complete baseline, clean-control, result and review evidence.
