# Independent review — T21

Review **Complete all six original public examples and controls** in a different session/person from its implementation author. Read [the worker brief](../workers/T21.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Finish the six selected cards with real bytes, mechanism, generator/rights, clean twin, actual named-reader output, coordinate references and preparation manifest. Include normal scan to counter alarming interpretation. Implement the gallery UI and its detail views as well as source PDFs, manifests and prepared reports; all cards must be backed by actual reader results.

## Task-specific review

- Every card source/hash/output manifest validates.
- no unexecuted expectation presented as result.
- supported languages accurately labeled.
- gallery not padded with repetitive cases.
- all assets rights-cleared and <static limit.

Use fixture families F01, F03, F04, F07, F12, F14 and invariants I01, I06, I13, I16, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/gallery/*.test.mjs && pnpm test:fixtures
```

## Scope and integrity

Check that implementation edits stay within `apps/web/public/examples/`, `fixtures/public/`, `scripts/prepare_examples.py`, `tests/gallery/`, `apps/web/src/features/gallery/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T21/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
