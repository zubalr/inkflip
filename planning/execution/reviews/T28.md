# Independent review — T28

Review **Implement bounded native structural observations** in a different session/person from its implementation author. Read [the worker brief](../workers/T28.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Expose documented text-object render mode, boxes and inspectable page properties with source locator and capability limits. Treat absent mode/property as unavailable; retain invisible searchable text without blanket alert.

## Task-specific review

- Invisible-over-border and white-on-dark controls cannot produce universal hidden/visible verdict.
- no false “all checked”.
- off-crop observation uses raw/native geometry while page display remains crop.
- unsupported compositing explicitly recorded.

Use fixture families F03, F04, F05, F06, F07 and invariants I04, I05, I06, I11. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
uv run --project native python -m pytest native/tests/structure -q
```

## Scope and integrity

Check that implementation edits stay within `native/inkflip/checks/structure.py`, `native/tests/structure/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T28/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
