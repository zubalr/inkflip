# Independent review — T40

Review **Verify native containment and failure recovery** in a different session/person from its implementation author. Read [the worker brief](../workers/T40.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Audit nonroot read-only mounts/no network/process groups/limits and fail-inject child hangs, crashes, stdout flood and temporary failures. Verify no untrusted deserialization or automatic plugin/model retrieval. Complete and exercise the recommended digest-pinned native container recipe and invocation; source-only portability cannot substitute for the documented containment profile.

## Task-specific review

- Source bytes unchanged.
- output cannot escape root.
- child/descendants terminated.
- successful unrelated files remain valid.
- unsupported OS limits reported.
- actual container no-network test uses owned synthetic files and records image digest.

Use fixture families F17, F19, F20, F21, F24 and invariants I01, I05, I10, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
uv run --project native python -m pytest native/tests/security -q && python tests/containment/run.py
```

## Scope and integrity

Check that implementation edits stay within `native/tests/security/`, `tests/containment/`, `artifacts/containment/`, `build/native/`, `scripts/run_native_container.sh`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T40/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
