# Independent review — T09

Review **Implement PDF.js rendering/text reader adapter** in a different session/person from its implementation author. Read [the worker brief](../workers/T09.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Use pinned paired legacy main/worker and documented getTextContent/getViewport. Preserve items/ordinal/transform/raw text and capability limits. Bound/yield rendering; never activate PDF actions, links or attachments.

## Task-specific review

- Real mapping and clean PDFs processed through bytes.
- raw text not injected.
- occurrence geometry/rotation/UserUnit verified.
- no full glyph-paint provenance claim.
- render cancellation releases task/canvas.
- unavailable structure check is unsupported.

Use fixture families F01, F02, F07, F08, F10, F11 and invariants I02, I04, I13, I14, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:browser -- tests/readers/pdfjs.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `packages/readers-pdfjs/`, `tests/readers/pdfjs.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T09/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
