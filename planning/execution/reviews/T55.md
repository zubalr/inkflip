# Independent review — T55

Review **Perform final adversarial merged-branch review** in a different session/person from its implementation author. Read [the worker brief](../workers/T55.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Review the full merged release candidate independently of its implementation authors across product, PDF geometry, browser, native regression, evaluation, security, deployment and integration. Reject duplicate active architectures, dead scaffolding, golden-test tampering, unsupported claims or mutation of the original repository. Verify every required capability has current evidence.

## Task-specific review

- All G1–G4 receipts are fresh for the actual release candidate.
- Static deployment preflight and documentation command checks pass.
- No public claim exceeds its linked evidence.
- The owner records visual approval of the working application.
- Only owner-controlled publication inputs remain before T54. Any defect creates a specific corrective task and invalidates the affected gate receipts.

Use fixture families F01, F03, F07, F11, F17, F20, F21, F23, F24, F25 and invariants I01, I02, I03, I04, I05, I06, I07, I08, I09, I10, I11, I12, I13, I14, I15, I16, I17, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python scripts/gate.py pre-release
```

## Scope and integrity

Check that implementation edits stay within `artifacts/final-review/`, `docs/release-candidate.md`, `tests/gates/final/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T55/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
