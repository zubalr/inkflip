# Independent review — T39

Review **Measure and enforce runtime/device performance budgets** in a different session/person from its implementation author. Read [the worker brief](../workers/T39.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Measure cold/warm/downloads/render/OCR/alignment/cancel and memory/cleanup on declared machines. Bound edge cases before allocation, clear/cycle20runs and quantify residual process memory versus tracked allocations.

## Task-specific review

- Reference targets either pass with raw receipts or block affected capability.
- no invented portable JS heap cap.
- job/raster/output budgets enforced.
- mobile fallback is visible.
- peak/tail timings and device identity reported, not synthetic elapsed animation.

Use fixture families F18, F19, F20, F21, F22 and invariants I05, I14, I17, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python scripts/measure_performance.py --profile reference-desktop && pnpm test:browser -- tests/performance/budgets.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `tests/performance/`, `scripts/measure_performance.py`, `artifacts/performance/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T39/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
