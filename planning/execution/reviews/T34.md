# Independent review — T34

Review **Implement stored-run comparison, rules and immutable baselines** in a different session/person from its implementation author. Read [the worker brief](../workers/T34.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Implement compare-readers, compare stored runs, acceptance-rule evaluation, baseline create/approval and complete exit semantics. Capture text/geometry/coverage differences without declaring correct/improved absent explicit rules.

## Task-specific review

- Changed no-rule is informational.
- rule regression exits5.
- lost coverage cannot count improvement.
- incompatiblefiles/readerconfig marked.
- baseline overwrite/auto-refresh prohibited.
- new baseline requires explicit approval and new path.

Use fixture families F23, F25, F26 and invariants I05, I12, I13. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:regression && uv run --project native python -m pytest native/tests/baselines -q
```

## Scope and integrity

Check that implementation edits stay within `packages/compare/regression/`, `native/inkflip/baselines/`, `tests/regression/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T34/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
