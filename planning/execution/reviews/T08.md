# Independent review — T08

Review **Implement local file validation and page/region selection** in a different session/person from its implementation author. Read [the worker brief](../workers/T08.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Handle input/drop/replace, MIME/header/size checks without uploading, metadata page bounds, explicit page selection and bounded region specification. Encrypted or malformed PDFs have distinct local errors; filenames remain local labels only.

## Task-specific review

- 20MiB/1000-page and mobile limits exercised without giant allocations.
- wrong MIME/header, encrypted and malformed are distinct.
- new file revokes old generation first.
- page selection never silently excludes a page.
- no document metadata in URL.

Use fixture families F17, F18, F19, F21 and invariants I01, I07, I08, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:browser -- tests/browser/open.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `apps/web/src/features/open/`, `apps/web/src/features/selection/`, `tests/browser/open.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T08/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
