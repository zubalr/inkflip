# Independent review — T13

Review **Integrate page/text/compare viewer and exact evidence navigation** in a different session/person from its implementation author. Read [the worker brief](../workers/T13.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Implement synchronized flip/side-by-side views, zoom/pan/rotation, selection, occurrence navigation and navigable text equivalent. Use canonical geometry, not normalized-value search, to connect highlights. Own root application composition and public Home/Workspace routes. Integrate the hero and report-import entry with the viewer; subsequent gallery/cache tasks propose changes to shared composition through its lease.

## Task-specific review

- Click/keyboard finding selects correct duplicate/page after zoom/rotation.
- two views sync without scroll loop.
- narrow screen stacks instead of squeezing.
- unknown geometry stays page-level.
- canvas has equivalent reachable content and limits.

Use fixture families F01, F07, F09, F11 and invariants I02, I04, I07, I14. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:browser -- tests/browser/viewer.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `apps/web/src/features/viewer/`, `tests/browser/viewer.spec.ts`, `apps/web/src/App.tsx`, `apps/web/src/pages/Home.tsx`, `apps/web/src/pages/Workspace.tsx`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T13/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
