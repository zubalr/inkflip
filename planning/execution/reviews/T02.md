# Independent review — T02

Review **Freeze actual dependency locks, model assets and build provenance** in a different session/person from its implementation author. Read [the worker brief](../workers/T02.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Install selected compatible packages in isolated environments with owner-authorized setup network; verify engines/advisories, exact package/model hashes and notices. Pin compatible patched toolchain builds and full action SHAs; stage all browser resources same-origin. Add exact pytest, browser accessibility and test-only dependencies to the lock with purpose.

## Task-specific review

- Frozen install succeeds twice from a clean checkout.
- every bundled binary has source/hash/license.
- substituted model fails checksum.
- missing asset fails closed.
- no runtime CDN/download.
- action/OCI revisions are immutable and recorded.

Use fixture families F20, F22 and invariants I13, I16. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python scripts/check_dependencies.py --frozen && python -m unittest discover -s tests/build -v
```

## Scope and integrity

Check that implementation edits stay within `pnpm-lock.yaml`, `native/uv.lock`, `.node-version`, `.python-version`, `config/resolved-assets.json`, `scripts/prepare_assets.py`, `scripts/check_dependencies.py`, `build/base-image.lock.json`, `package.json`, `native/pyproject.toml`, `apps/web/package.json`, `config/test-toolchain.json`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T02/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
