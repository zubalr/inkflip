# Independent review — T01

Review **Bootstrap the separate monorepo and honest origin** in a different session/person from its implementation author. Read [the worker brief](../workers/T01.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Create local package boundaries, the nonempty-test command harness, copy planning inputs, and document snapshot provenance without copying legacy adjudication. Initialize early read-only CI; no remote, deployment or dependency install is implicit. Create the minimal Vite/React entry and module build boundaries needed by T02/T06/T13. Keep the scaffold explicitly unfinished and local; do not publish empty or simulated product controls. Retain the exact original license at the documented origin path.

## Task-specific review

- A clean checkout exposes documented commands.
- empty/missing test registration fails.
- old repository remains byte/Git-unchanged.
- no network action or generated achievement claim occurs.

Use fixture families F01 and invariants I01, I16. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python -m unittest discover -s tests/bootstrap -v
```

## Scope and integrity

Check that implementation edits stay within `package.json`, `pnpm-workspace.yaml`, `native/pyproject.toml`, `scripts/task_acceptance.py`, `scripts/gate.py`, `.github/workflows/ci.yml`, `docs/ORIGIN.md`, `apps/web/package.json`, `apps/web/index.html`, `apps/web/vite.config.ts`, `apps/web/tsconfig.json`, `apps/web/src/main.tsx`, `apps/web/src/App.tsx`, `third_party/origin/mib-intake/LICENSE`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T01/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
