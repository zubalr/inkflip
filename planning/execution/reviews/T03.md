# Independent review — T03

Review **Implement schema, strict validation, canonical identity and generated types** in a different session/person from its implementation author. Read [the worker brief](../workers/T03.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Generate types/static Ajv from the single schema; port semantic validation and canonical hash exactly. Reject duplicate keys, lone surrogates, nonfinite/unsafe values, unknown versions and broken references. Keep explicit migrations separate.

## Task-specific review

- All valid/invalid fixtures and expected failure codes pass in TS/Python.
- all hash vectors agree.
- type generation is deterministic.
- untrusted report never compiles a schema.
- array order and hash-excluded timestamps behave as specified.

Use fixture families F24, F25, F26 and invariants I02, I03, I05, I10, I13. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/contracts/*.test.mjs && uv run --project native python -m pytest native/tests/contracts -q
```

## Scope and integrity

Check that implementation edits stay within `packages/contracts/`, `native/inkflip/contracts/`, `scripts/generate_contracts.py`, `tests/contracts/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T03/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
