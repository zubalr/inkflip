# Independent review — T12

Review **Implement raw normalization and conservative alignment** in a different session/person from its implementation author. Read [the worker brief](../workers/T12.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Implement scalar-whitespace-v1 raw maps and region-match-v1 bounded matching with split/merge provenance, duplicate occurrence candidates, order differences and ambiguity abstention. Export score components as algorithm diagnostics, not probability.

## Task-specific review

- Digits/currency/sign/negation/combining marks cannot disappear.
- four repeated equal values remain four occurrences.
- ambiguous ties abstain.
- page-only inputs never become exact regional highlights.
- no unbounded quadratic matching.

Use fixture families F11, F12, F13, F14, F15 and invariants I02, I03, I04, I06, I11. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/alignment/*.test.mjs
```

## Scope and integrity

Check that implementation edits stay within `packages/compare/normalization/`, `packages/compare/alignment/`, `tests/alignment/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T12/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
