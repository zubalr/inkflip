# Independent review — T04

Review **Implement canonical geometry and transform conformance** in a different session/person from its implementation author. Read [the worker brief](../workers/T04.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Implement C/R/S/crop composition, inverse, clipping metadata and explicit precision; UserUnit and effective box are applied once. Add independently derived expected coordinates rather than only invert-own-output tests.

## Task-specific review

- All four rotations/nonzero origins/UserUnit worked examples pass <=1e-5pt.
- singular transforms rejected.
- estimated/page-only polygons obey schema.
- CSS/DPR tests verify correct page and p95<=2px, maximum 4 px once viewer integrates.

Use fixture families F07, F08, F09, F10 and invariants I02, I04. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/geometry/*.test.mjs
```

## Scope and integrity

Check that implementation edits stay within `packages/geometry/`, `tests/geometry/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T04/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
