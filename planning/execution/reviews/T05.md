# Independent review — T05

Review **Create original fixture foundation and rights manifest** in a different session/person from its implementation author. Read [the worker brief](../workers/T05.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Port the original fixed harmless generator and develop controlled public/development families; retain exact generator intent, expected raw reader outputs and clean twins. Keep evaluation labels outside optimization checkout. Include the legitimate searchable-scan F03 minimum control before G1; the full gallery presentation remains T21.

## Task-specific review

- Repeated generation is byte-identical or declared pinned asset-dependent.
- mapping/control render equal under same environment and actual extraction differs.
- no filename-conditioned app output.
- no embedded unlicensed font or private/challenge answer data.
- An original legitimate searchable scan (F03) is generated and read without an alarm solely because its OCR text is invisible; this minimum control is present before G1.

Use fixture families F01, F02, F07, F08, F03 and invariants I01, I13, I16. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python scripts/make_fixtures.py --check && python -m unittest discover -s tests/fixtures -v
```

## Scope and integrity

Check that implementation edits stay within `fixtures/public/`, `fixtures/development/`, `scripts/make_fixtures.py`, `fixtures/manifest.json`, `tests/fixtures/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T05/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
