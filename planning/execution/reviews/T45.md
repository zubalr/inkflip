# Independent review — T45

Review **Evaluate original-preserving raster geometry only** in a different session/person from its implementation author. Read [the worker brief](../workers/T45.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Run P14 on a single bounded reversible deskew/registration candidate and negative ruled-page controls. Original PDF/raster immutable; candidate output is separately labeled interpretation with inverse chain.

## Task-specific review

- Any lost registration or clean corruption rejects.
- gain must transfer beyond the challenge generator.
- no generic reconstruction/search or PDF write path.
- negative result closes task with deleted production candidate.

Use fixture families F06, F07, F09, F15 and invariants I01, I02, I04, I16. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python experiments/P14/run.py --manifest evaluation/manifests/development.json --out artifacts/P14 && python experiments/P14/test_experiment.py
```

## Scope and integrity

Check that implementation edits stay within `experiments/P14/`, `artifacts/P14/`, `docs/experiments/P14.md`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T45/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
