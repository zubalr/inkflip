# Independent review — T46

Review **Verify browser/native and cross-environment parity** in a different session/person from its implementation author. Read [the worker brief](../workers/T46.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Execute P15 repeated fixed-environment runs and supported-browser/native comparison. Distinguish expected engine differences from hash/normalization/coverage drift, cold model cache from warm and version upgrades from same-version repeats.

## Task-specific review

- No unexplained difference in promised semantic equivalence.
- required hash vectors same everywhere.
- engine renders not forced equal.
- exact build/CPU/OS/model manifest and all failed/skipped profiles recorded.

Use fixture families F01, F07, F08, F23, F25 and invariants I02, I03, I12, I13, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/parity/*.test.mjs && python scripts/check_manual_receipts.py compatibility
```

## Scope and integrity

Check that implementation edits stay within `tests/parity/`, `artifacts/P15/`, `docs/compatibility/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T46/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
