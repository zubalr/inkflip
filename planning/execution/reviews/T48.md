# Independent review — T48

Review **Validate static build, CSP, caching and deployment preflight** in a different session/person from its implementation author. Read [the worker brief](../workers/T48.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Build asset-only release and prove no Worker main/dynamic/API/bindings/SSR; local Wrangler dryrun/preview headers and core worker/WASM/CSP behavior. No paid resource is deployed by this task.

## Task-specific review

- Actual build has only static assets.
- each asset<24MiB and rights and hash manifest complete.
- routes/deeplinks/missingassets expected.
- no third-party requests.
- wasm/CSP works without script unsafe-eval.
- default no COOP/COEP/SharedArrayBuffer requirement.

Use fixture families F20, F22, F24 and invariants I08, I13, I15, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm build && python scripts/check_static_dist.py apps/web/dist --config wrangler.json && python -m unittest discover -s tests/deployment -v
```

## Scope and integrity

Check that implementation edits stay within `wrangler.json`, `apps/web/public/_headers`, `scripts/check_static_dist.py`, `tests/deployment/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T48/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
