# Independent review — T17

Review **Earn the browser amount demo and prepared manifest** in a different session/person from its implementation author. Read [the worker brief](../workers/T17.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Run actual pinned browser readers on the generated amount/twin and store source-linked prepared output with manifest. Bind UI to ordinary report data, not a fixture filename/expected-value conditional.

## Task-specific review

- Renamed identical bytes produce same reading.
- modified bytes cannot replay old prepared result.
- live and prepared paths label provenance correctly.
- real source downloadable.
- actual timing visible, not decorative scan animation.
- clean counterpart renders equal under same renderer.

Use fixture families F01, F02 and invariants I01, I02, I13, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:browser -- tests/browser/amount.spec.ts && pnpm test:fixtures
```

## Scope and integrity

Check that implementation edits stay within `apps/web/public/examples/amount/`, `scripts/prepare_examples.py`, `tests/browser/amount.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T17/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
