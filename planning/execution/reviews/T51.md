# Independent review — T51

Review **Close complete public investigation capability gate** in a different session/person from its implementation author. Read [the worker brief](../workers/T51.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Run G2 against the merged, complete browser application: own-file processing, page and region selection, normal controls, repeated and ambiguous occurrences, the real gallery, local report import/export, privacy preview and explicit offline behavior. Exercise the built artifact, not just individual component tests.

## Task-specific review

- All mandatory public features work without native/backend.
- all controls functional.
- missing-model and unsupported results remain partial, never clean.
- tests run against built artifact with exact versions.
- defects reopen owners.

Use fixture families F01, F03, F07, F11, F14, F17, F24 and invariants I01, I02, I03, I04, I05, I06, I07, I08, I09, I10, I14. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python scripts/gate.py G2
```

## Scope and integrity

Check that implementation edits stay within `tests/gates/g2.spec.ts`, `artifacts/gates/G2/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T51/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
