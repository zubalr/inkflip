# Independent review — T29

Review **Implement native parent supervision and atomic partial results** in a different session/person from its implementation author. Read [the worker brief](../workers/T29.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Spawn disposable reader processes with explicit wall/memory/output limits and process-group cleanup; use atomic per-file writes and an immutable journal. Default jobs1; keep PDFium calls out of concurrent threads.

## Task-specific review

- Injected Python/native-like hang/crash/OOM/output flood gets classified and bounded.
- successful files remain byte-stable.
- one retry max within deadline.
- empty success not retried.
- Ctrl-C terminates descendants and leaves valid partial artifact.

Use fixture families F17, F18, F19, F20, F21 and invariants I05, I07, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
uv run --project native python -m pytest native/tests/runtime -q
```

## Scope and integrity

Check that implementation edits stay within `native/inkflip/runtime/`, `native/tests/runtime/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T29/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
