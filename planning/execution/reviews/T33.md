# Independent review — T33

Review **Implement explicit version-isolated reader profiles** in a different session/person from its implementation author. Read [the worker brief](../workers/T33.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Implement explicitly invoked local profile setup for incompatible versions in separate virtual environments, trusted allowlisted reader wrappers and recorded executable/environment identity. Setup can download only on operator command, never on report import/replay. Also provide the fixed Node PDF.js profile wrapper inside packages/readers-pdfjs/node; use the shared PDF.js adapter and separate Node workspace locks. This is a named-reader profile, not arbitrary executable discovery.

## Task-specific review

- Old/new pypdf versions demonstrably loaded from distinct interpreters.
- reader version printed in outputs.
- missing version install blocks that profile.
- untrusted profile path/command refused.
- offline run makes no requests.

Use fixture families F23, F24, F25 and invariants I10, I13, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
uv run --project native python -m pytest native/tests/profiles -q
```

## Scope and integrity

Check that implementation edits stay within `scripts/install_reader_profile.py`, `native/inkflip/profiles/`, `native/tests/profiles/`, `packages/readers-pdfjs/node/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T33/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
