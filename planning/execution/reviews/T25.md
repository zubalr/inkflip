# Independent review — T25

Review **Implement explicit offline static/model cache lifecycle** in a different session/person from its implementation author. Read [the worker brief](../workers/T25.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Cache only versioned allowlisted application/example/model assets with explicit prepare-offline action. Never persist documents/reports by default; document reset differs from remove-offline-assets. Cache generation is manifest-bound.

## Task-specific review

- Cold/offline without model reports unavailable.
- prepared offline own-file works without requests.
- model update cannot reuse wrong hash.
- clear terminates workers/revokes URLs/removes requested caches, with no forensic erasure promise.

Use fixture families F20, F22, F24 and invariants I08, I13, I14, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:privacy -- tests/privacy/cache.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `apps/web/src/offline/`, `apps/web/public/sw.js`, `tests/privacy/cache.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T25/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
