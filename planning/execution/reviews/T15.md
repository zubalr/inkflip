# Independent review — T15

Review **Prove initial own-file no-egress behavior** in a different session/person from its implementation author. Read [the worker brief](../workers/T15.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Run canary PDF through actual built browser flow including error, export and reopen. Capture all requests/websocket/beacon/worker/service-worker routes and storage, filenames/hashes/query fragments/console payloads; assess cold fixed assets separately.

## Task-specific review

- No canary bytes/text/name/hash/crop/report in outbound traffic or URLs.
- only fixed allowlisted same-origin asset paths.
- telemetry/external-font absence proven.
- online/warm/offline modes documented.
- redact private test logs before public artifacts.

Use fixture families F24, F22 and invariants I08, I09, I10, I14. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:privacy -- tests/privacy/local.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `tests/privacy/`, `scripts/inspect_network_receipt.py`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T15/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
