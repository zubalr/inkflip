# Independent review — T36

Review **Build independent evaluation protocol and held-out corpus** in a different session/person from its implementation author. Read [the worker brief](../workers/T36.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Freeze grouped development/public/evaluation splits, generator families and truth labels under independent custodian control. Implement precise denominators, confidence intervals, clean/damaged metrics and diagnosis test forms. Tuning workers do not get untouched labels.

## Task-specific review

- No sibling leakage.
- skipped/failed pages remain coverage denominator.
- repeated OCR readings are not independent samples.
- targets reported as unmet until real run.
- small real incident data stays permissioned and out of public logs.

Use fixture families F01, F03, F04, F11, F12, F14, F15, F23 and invariants I06, I12, I16, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python -m unittest discover -s tests/evaluation -v
```

## Scope and integrity

Check that implementation edits stay within `evaluation/protocol/`, `evaluation/manifests/`, `scripts/evaluate_inspector.py`, `tests/evaluation/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T36/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
