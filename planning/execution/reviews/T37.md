# Independent review — T37

Review **Complete accessibility and manual assistive-technology review** in a different session/person from its implementation author. Read [the worker brief](../workers/T37.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Run all A01–A14 acceptance cases including keyboard, dialogs, page/text alternatives, zoom/reflow and screen-reader flows. Record actual browser/OS/AT versions and failures, not inferred conformance.

## Task-specific review

- All core flows completed without mouse.
- focus restored.
- amount alternatives and partial coverage announced correctly.
- 400% zoom usable.
- reduced motion honored.
- zero serious/critical automated and zero blocked manual critical flow.

Use fixture families F01, F03, F11, F17, F21 and invariants I04, I06, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:a11y && python scripts/check_manual_receipts.py accessibility
```

## Scope and integrity

Check that implementation edits stay within `tests/a11y/`, `docs/accessibility/`, `artifacts/a11y/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T37/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
