# Independent review — T24

Review **Harden malicious report, text and image boundaries** in a different session/person from its implementation author. Read [the worker brief](../workers/T24.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Implement strict duplicate-key/depth/string/decoded-asset accounting, PNG full decode/reencode with pixel/edge limits, MIME/signature/hash checks, safe names and prototype-key handling. Imported executable/plugin fields are rejected by contract.

## Task-specific review

- Controlled oversized/truncated/HTML/SVG/polyglot/prototype/path inputs fail before dangerous allocation or resource use.
- bounded fuzz receipts show no unexpected accepts/crashes/egress.
- HTML export opens script-free.
- no archive decompression surface.

Use fixture families F24, F25, F26, F19 and invariants I09, I10, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/security/import/*.test.mjs && python scripts/fuzz_reports.py --fixed-seed 8090 --cases 1000
```

## Scope and integrity

Check that implementation edits stay within `packages/reports/validation/`, `tests/security/import/`, `scripts/fuzz_reports.py`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T24/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
