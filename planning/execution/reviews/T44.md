# Independent review — T44

Review **Evaluate one native RapidOCR complement** in a different session/person from its implementation author. Read [the worker brief](../workers/T44.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Execute P13 with RapidOCR v3.8.1 and one PP-OCRv5 mobile English recognizer/detector configuration as the selected experimental candidate, in a separate native environment. Resolve exact official weight artifacts, hashes and rights before running; do not reuse unexplained ONNX files from a challenge checkout. Compare its useful findings and resource costs with the default native Tesseract path over the same raster. Shared pixels are not independent ground truth. A failed provenance or value gate leaves RapidOCR unavailable.

## Task-specific review

- Complementary useful failures at fixed precision and memory budget or reject.
- explicit missing-model and OOV behavior.
- no runtime network.
- no consensus-as-truth.
- full raw and negative outcomes documented.

Use fixture families F14, F15, F16 and invariants I06, I13, I16, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python experiments/P13/run.py --manifest evaluation/manifests/development.json --out artifacts/P13 && python experiments/P13/test_experiment.py
```

## Scope and integrity

Check that implementation edits stay within `experiments/P13/`, `artifacts/P13/`, `docs/experiments/P13.md`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T44/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
