# Independent review — T11

Review **Implement run lifecycle, backpressure and cancellation** in a different session/person from its implementation author. Read [the worker brief](../workers/T11.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Implement explicit state machine, per-check terminal bookkeeping, generation/digest/run/job/sequence authority, bounded acknowledged result chunks, retry reasons and cleanup. Do not clear successfully completed independent page results on one error.

## Task-specific review

- Stale results after replace/cancel cannot render.
- 256-occurrence chunks and two unacked cap enforced.
- cancelled/partial/failed differ.
- empty success not retried.
- UI cancellation target of 100 ms and termination target of 500 ms measured in integration.

Use fixture families F20, F21, F22 and invariants I05, I07, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/runtime/*.test.mjs
```

## Scope and integrity

Check that implementation edits stay within `packages/runtime/`, `apps/web/src/state/`, `tests/runtime/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T11/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
