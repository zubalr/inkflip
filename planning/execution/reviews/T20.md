# Independent review — T20

Review **Finish repeated, ambiguous, order and unmatched evidence UX** in a different session/person from its implementation author. Read [the worker brief](../workers/T20.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Expose all candidate occurrences, split/merge raw spans, original reading order and page-level missingness. Add user navigation to ambiguity without user annotations silently changing machine findings.

## Task-specific review

- Four repeated amounts never jump to first string match.
- order-only differences remain order-only.
- incomplete extraction cannot be claimed missing visible text.
- keyboard selection returns to originating finding.

Use fixture families F11, F12, F13, F16 and invariants I02, I03, I04, I06. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:browser -- tests/browser/ambiguity.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `apps/web/src/features/viewer/occurrences/`, `apps/web/src/features/findings/alignment/`, `tests/browser/ambiguity.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T20/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
