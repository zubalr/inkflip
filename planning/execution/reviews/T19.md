# Independent review — T19

Review **Complete large-document and narrow-device interaction** in a different session/person from its implementation author. Read [the worker brief](../workers/T19.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Add progressive metadata/page picker, explicit unselected coverage, low-memory mode and mobile OCR consent. Useful text/preview must remain available while heavy work is refused or pending.

## Task-specific review

- 1,000-page metadata plan does not render all pages.
- selected native/OCR limits visible.
- 320px and touch controls usable.
- oversized edges rejected before allocation.
- cancellation leaves coherent partial result and next-file health.

Use fixture families F18, F19, F21 and invariants I05, I14, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:browser -- tests/browser/large-mobile.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `apps/web/src/features/selection/`, `apps/web/src/features/open/`, `tests/browser/large-mobile.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T19/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
