# Independent review — T32

Review **Implement corpus run, journal and validated resume** in a different session/person from its implementation author. Read [the worker brief](../workers/T32.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Read root-scoped manifest with path containment/no symlinks, hash every source, isolate files and write per-file reports plus run index atomically. Resume only verified same input/profile/config completed records.

## Task-specific review

- Duplicate filenames remain distinct corpus keys.
- traversal/symlink escape rejected.
- altered bytes invalidate resume.
- one crashing file cannot erase successful reports.
- deterministic index ordering and explicit partial counts survive interruption.

Use fixture families F17, F21, F23, F25 and invariants I01, I05, I12, I13, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
uv run --project native python -m pytest native/tests/corpus -q
```

## Scope and integrity

Check that implementation edits stay within `native/inkflip/corpus/`, `native/tests/corpus/`, `docs/CORPUS.md`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T32/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
