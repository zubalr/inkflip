# Independent review — T43

Review **Evaluate secondary browser reader, decide explicitly** in a different session/person from its implementation author. Read [the worker brief](../workers/T43.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Execute P12 with the EmbedPDF PDFium package from release v2.15.0 as the selected experimental candidate. Verify the release asset digest, package API, geometry and complete notices, then compare the same original files with the default PDF.js reader. Do not install the full viewer or add a second download to the public path unless the experiment passes and a separate integration review approves it.

## Task-specific review

- Useful complementary mechanism gain at fixed precision and bounded download/RSS or reject.
- API/geometry proven.
- every asset<24MiB.
- raw results, negative controls and rights included.
- default unavailable if candidate fails.

Use fixture families F01, F07, F10, F11 and invariants I04, I13, I14, I16, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python experiments/P12/check_manifest.py && pnpm test:browser -- experiments/P12/browser.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `experiments/P12/`, `artifacts/P12/`, `docs/experiments/P12.md`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T43/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
