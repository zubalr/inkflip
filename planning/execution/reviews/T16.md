# Independent review — T16

Review **Implement portable JSON and escaped HTML export foundation** in a different session/person from its implementation author. Read [the worker brief](../workers/T16.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Build selected evidence JSON and script-free HTML using an inclusion allowlist and pre-export preview. Include raw mappings, check denominators, engine/settings/hashes/limits; source PDF/name/notes off by default. No ZIP format or HTML import.

## Task-specific review

- Preview equals decoded contents.
- raw text/geometry/coverage survive.
- no source PDF without explicit opt-in.
- script-like strings escaped.
- missing bytes marked evidence-only.
- export is deterministic aside from excluded run timing fields.

Use fixture families F01, F24, F25 and invariants I02, I05, I09, I10, I13. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/reports/export.test.mjs
```

## Scope and integrity

Check that implementation edits stay within `packages/reports/export/`, `apps/web/src/features/export/`, `tests/reports/export.test.mjs`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T16/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
