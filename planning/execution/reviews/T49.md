# Independent review — T49

Review **Finish user/developer docs and honest limitations** in a different session/person from its implementation author. Read [the worker brief](../workers/T49.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Create public README, quickstart, offline/native install, example commands, report/privacy semantics, capability/support limits, architecture map and actual development history. Every screenshot/version/command must match shipped behavior.

## Task-specific review

- Clean-machine instructions and all commands exercised.
- no unsupported “safe/fraud/AI sees”/score/agent autonomy claims.
- repeated-reader workflow documented.
- any unknown rights and owner inputs plainly separated.
- links valid.

Use fixture families F01, F03, F23, F25 and invariants I06, I13, I16, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python -m unittest discover -s tests/docs -v && python scripts/check_claims.py
```

## Scope and integrity

Check that implementation edits stay within `README.md`, `docs/quickstart.md`, `docs/user-guide.md`, `docs/developer-guide.md`, `docs/architecture.md`, `docs/limitations.md`, `docs/development-history.md`, `apps/web/src/pages/help/`, `tests/docs/`, `scripts/check_claims.py`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T49/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
