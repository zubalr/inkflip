# Independent review — T26

Review **Implement native PDFium and pypdf reader adapters** in a different session/person from its implementation author. Read [the worker brief](../workers/T26.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Use documented PDFium character APIs/boxes and pypdf metadata/text paths. Preserve one-to-many glyph mapping and repeated occurrences. Explicitly compensate tested PDFium UserUnit handling and avoid cross-thread library calls.

## Task-specific review

- Mapping/control/covered actual outputs preserved.
- shared glyph boxes not split with guessed widths.
- pypdf text stays page-only.
- all rotations/origins map correctly.
- malformed/encrypted failures isolated.
- build/version manifest complete.

Use fixture families F01, F02, F07, F08, F10, F11 and invariants I02, I04, I13, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
uv run --project native python -m pytest native/tests/readers -q
```

## Scope and integrity

Check that implementation edits stay within `native/inkflip/readers/pdfium.py`, `native/inkflip/readers/pypdf.py`, `native/tests/readers/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T26/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
