# Independent review — T23

Review **Finish export selection, annotations and privacy preview** in a different session/person from its implementation author. Read [the worker brief](../workers/T23.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Complete multi-finding selection, explicit note inclusion, original-byte opt-in, crop preview and replay-level disclosure. Human notes never become machine readings or overwrite evidence.

## Task-specific review

- Default selected finding does not include unrelated text/name/PDF/notes.
- full-source warning requires explicit choice.
- crop is described as excerpt not safe redaction.
- selected omissions retain honest original produced_count and check coverage.

Use fixture families F11, F24, F25 and invariants I06, I09, I10. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:browser -- tests/reports/selection.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `apps/web/src/features/export/`, `apps/web/src/features/annotations/`, `tests/reports/selection.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T23/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
