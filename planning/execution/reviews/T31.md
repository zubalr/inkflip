# Independent review — T31

Review **Implement shared Node comparison bridge** in a different session/person from its implementation author. Read [the worker brief](../workers/T31.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Package shared TS comparison as a fixed installed Node entrypoint; Python calls fixed argv with bounded local JSON messages. Browser/native compare use identical normalized/geometry semantics, not separate reimplementations.

## Task-specific review

- Golden cross-language comparison cases agree exactly.
- missing Node yields clear capability error, not fallback algorithm.
- stdout bounded.
- no shell/plugin/profile selection from report.
- manifests retain comparator version.

Use fixture families F11, F12, F23, F25 and invariants I03, I04, I10, I12, I13. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
node --test tests/bridge/*.test.mjs && uv run --project native python -m pytest native/tests/bridge -q
```

## Scope and integrity

Check that implementation edits stay within `packages/compare/node/`, `native/inkflip/compare_bridge.py`, `tests/bridge/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T31/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
