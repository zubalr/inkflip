# Independent review — T54

Review **Owner-authorized static deployment, rollback and final release** in a different session/person from its implementation author. Read [the worker brief](../workers/T54.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** After explicit owner approval, deploy exact static artifact to supplied account/domain, test real request paths/headers/no-egress/account-level script injection, practice rollback to an exact archived artifact, tag release and publish only approved source and media. No unrelated paid service is enabled.

## Task-specific review

- The public sample and own-file workflow work on the actual authorized origin.
- Deployment configuration, request-path tests and available account metrics establish that application traffic does not invoke a dynamic Worker.
- No upload endpoint or account-injected analytics script is present.
- Rollback serves the previously archived artifact and its verified hashes.
- The archived distribution, model manifest, SBOM, source, test receipts and public claims match the release.
- The owner records explicit publication approval without disclosing secrets.

Use fixture families F01, F03, F20, F22, F24 and invariants I08, I13, I15, I16, I18. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
python scripts/gate.py G5 --target "$INKFLIP_PUBLIC_ORIGIN"
```

## Scope and integrity

Check that implementation edits stay within `artifacts/gates/G5/`, `artifacts/releases/`, `docs/releases/`, `tests/deployment/public.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T54/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
