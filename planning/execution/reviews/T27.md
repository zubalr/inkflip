# Independent review — T27

Review **Implement native rendered-region Tesseract reader** in a different session/person from its implementation author. Read [the worker brief](../workers/T27.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Invoke fixed Tesseract argv without shell, over bounded named-renderer PNGs; use exact local model and word TSV hierarchy/geometry. Keep settings/confidence as engine diagnostics and crop transforms attached.

## Task-specific review

- No URL or runtime model fetch.
- missing model/binary/unreadable page differ.
- malicious filename cannot become an option.
- crop padding/resize inverses correct.
- native OCR retains raw punctuation and all occurrences.

Use fixture families F01, F03, F14, F15, F20 and invariants I02, I04, I13, I17. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
uv run --project native python -m pytest native/tests/ocr -q
```

## Scope and integrity

Check that implementation edits stay within `native/inkflip/readers/tesseract.py`, `native/tests/ocr/`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T27/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
