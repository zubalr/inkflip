# Independent review — T22

Review **Implement strict local JSON import and reopen flow** in a different session/person from its implementation author. Read [the worker brief](../workers/T22.md), the canonical task, changed source, input contracts, fixture expectations and actual receipts. A green badge or implementation summary alone is insufficient.

**Expected purpose:** Parse only bounded canonical JSON, validate all refs/digests/asset signatures, require explicit local source for replay, and display scope/omissions. Refuse HTML/ZIP/schema versions/external pointers; clear previous generation first.

## Task-specific review

- Real selected export reopens without upload.
- source omission and unavailable reader explicit.
- malicious report cannot fetch/execute/select profile.
- hash mismatch blocks replay.
- comparison asks for exact locally selected source reports.

Use fixture families F24, F25, F26 and invariants I02, I05, I07, I09, I10. Inspect a negative control and an error/partial path relevant to this task, not only a happy example.

```sh
pnpm test:browser -- tests/reports/import.spec.ts
```

## Scope and integrity

Check that implementation edits stay within `packages/reports/import/`, `apps/web/src/features/import/`, `tests/reports/import.spec.ts`, plus the common evidence-only namespaces. Verify approved shared leases, exact contract/dependency identities, complete attribution and no undeclared network or persistence. Reject deleted assertions, unjustified skips, refreshed baselines that hide failure, or tuning on held-out labels. No unrelated SaaS, repair or adjudication code.

## Required disposition

Write `artifacts/tasks/T22/review.md`: reviewed commit, reviewer/session identity, commands actually executed, test counts and skips, source/fixture/model identities, issues with severity and reproduction, and accept/request-changes/blocked. Explicitly label inspection-only areas. A material assertion without evidence is not accepted.

Use the task's proposal procedure for plan conflicts. The integrator repeats affected acceptance on the merge result; branch-local success does not certify integration. Preserve original bytes and rejected-experiment receipts. Rollback reverts candidate changes, never rewrites a baseline to erase a failure.
