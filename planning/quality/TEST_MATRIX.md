# Executable acceptance test matrix

Canonical records: [test-matrix.json](test-matrix.json). These are **specified product tests**, not test results. The separately recorded planning-utility tests do not satisfy these release tests. Gate-owner rows list direct scenarios so a gate runner never recursively invokes itself.

| Test | Task / level | Command | Assertions |
|---|---|---|---|
| TEST-01 | T01 / unit_contract | `python -m unittest discover -s tests/bootstrap -v` | A clean checkout exposes documented commands; empty/missing test registration fails; old repository remains byte/Git-unchanged; no network action or generated achievement claim occurs. |
| TEST-02 | T02 / unit_contract | `python scripts/check_dependencies.py --frozen && python -m unittest discover -s tests/build -v` | Frozen install succeeds twice from a clean checkout; every bundled binary has source/hash/license; substituted model fails checksum; missing asset fails closed; no runtime CDN/download; action/OCI revisions are immutable and recorded. |
| TEST-03 | T03 / integration | `node --test tests/contracts/*.test.mjs && uv run --project native python -m pytest native/tests/contracts -q` | All valid/invalid fixtures and expected failure codes pass in TS/Python; all hash vectors agree; type generation is deterministic; untrusted report never compiles a schema; array order and hash-excluded timestamps behave as specified. |
| TEST-04 | T04 / unit_contract | `node --test tests/geometry/*.test.mjs` | All four rotations/nonzero origins/UserUnit worked examples pass <=1e-5pt; singular transforms rejected; estimated/page-only polygons obey schema; CSS/DPR tests verify correct page and p95<=2px, maximum 4 px once viewer integrates. |
| TEST-05 | T05 / unit_contract | `python scripts/make_fixtures.py --check && python -m unittest discover -s tests/fixtures -v` | Repeated generation is byte-identical or declared pinned asset-dependent; mapping/control render equal under same environment and actual extraction differs; no filename-conditioned app output; no embedded unlicensed font or private/challenge answer data.; An original legitimate searchable scan (F03) is generated and read without an alarm solely because its OCR text is invisible; this minimum control is present before G1. |
| TEST-06 | T06 / integration | `pnpm test:visual -- tests/visual/foundation.spec.ts` | 1440/1024/768/390/320 widths have no page overflow; page and disagreement dominate; contrast checks meet stated targets; reduced motion disables flips; screenshot evidence covers dark text, focus and partial states. |
| TEST-07 | T07 / integration | `pnpm test:a11y -- tests/a11y/primitives.spec.ts` | Keyboard can reach/operate all controls; Escape/focus return works; no focus trap outside modal; dynamic progress does not announce every OCR token; no serious/critical automated violations in primitives. |
| TEST-08 | T08 / integration | `pnpm test:browser -- tests/browser/open.spec.ts` | 20MiB/1000-page and mobile limits exercised without giant allocations; wrong MIME/header, encrypted and malformed are distinct; new file revokes old generation first; page selection never silently excludes a page; no document metadata in URL. |
| TEST-09 | T09 / integration | `pnpm test:browser -- tests/readers/pdfjs.spec.ts` | Real mapping and clean PDFs processed through bytes; raw text not injected; occurrence geometry/rotation/UserUnit verified; no full glyph-paint provenance claim; render cancellation releases task/canvas; unavailable structure check is unsupported. |
| TEST-10 | T10 / integration | `pnpm test:browser -- tests/readers/tesseract.spec.ts` | Actual English crop OCR emitted with engine/settings/model identity; no default CDN; initialization failure != unreadable page; bounded raster/edge/pixel counts; cached/cold/offline distinguished; low confidence never asserted as ground truth. |
| TEST-11 | T11 / unit_contract | `node --test tests/runtime/*.test.mjs` | Stale results after replace/cancel cannot render; 256-occurrence chunks and two unacked cap enforced; cancelled/partial/failed differ; empty success not retried; UI cancellation target of 100 ms and termination target of 500 ms measured in integration. |
| TEST-12 | T12 / unit_contract | `node --test tests/alignment/*.test.mjs` | Digits/currency/sign/negation/combining marks cannot disappear; four repeated equal values remain four occurrences; ambiguous ties abstain; page-only inputs never become exact regional highlights; no unbounded quadratic matching. |
| TEST-13 | T13 / integration | `pnpm test:browser -- tests/browser/viewer.spec.ts` | Click/keyboard finding selects correct duplicate/page after zoom/rotation; two views sync without scroll loop; narrow screen stacks instead of squeezing; unknown geometry stays page-level; canvas has equivalent reachable content and limits. |
| TEST-14 | T14 / integration | `pnpm test:browser -- tests/browser/findings.spec.ts` | Normal invisible scan has no warning solely for invisibility; timed-out/model-missing/unsupported are distinct; zero findings cannot display clean/safe; explanations come from deterministic templates and actual recorded evidence, not generated truth claims. |
| TEST-15 | T15 / unit_contract | `pnpm test:privacy -- tests/privacy/local.spec.ts` | No canary bytes/text/name/hash/crop/report in outbound traffic or URLs; only fixed allowlisted same-origin asset paths; telemetry/external-font absence proven; online/warm/offline modes documented; redact private test logs before public artifacts. |
| TEST-16 | T16 / unit_contract | `node --test tests/reports/export.test.mjs` | Preview equals decoded contents; raw text/geometry/coverage survive; no source PDF without explicit opt-in; script-like strings escaped; missing bytes marked evidence-only; export is deterministic aside from excluded run timing fields. |
| TEST-17 | T17 / integration | `pnpm test:browser -- tests/browser/amount.spec.ts && pnpm test:fixtures` | Renamed identical bytes produce same reading; modified bytes cannot replay old prepared result; live and prepared paths label provenance correctly; real source downloadable; actual timing visible, not decorative scan animation; clean counterpart renders equal under same renderer. |
| TEST-18 | T18 / gate | `pnpm exec playwright test tests/gates/g1.spec.ts` | Every G1 assertion has committed source and fresh receipt; no mocked own-file processor, backend, stale screenshot or skipped privacy check. Defects reopen owning tasks; complete downstream plan remains required. |
| TEST-19 | T19 / integration | `pnpm test:browser -- tests/browser/large-mobile.spec.ts` | 1,000-page metadata plan does not render all pages; selected native/OCR limits visible; 320px and touch controls usable; oversized edges rejected before allocation; cancellation leaves coherent partial result and next-file health. |
| TEST-20 | T20 / integration | `pnpm test:browser -- tests/browser/ambiguity.spec.ts` | Four repeated amounts never jump to first string match; order-only differences remain order-only; incomplete extraction cannot be claimed missing visible text; keyboard selection returns to originating finding. |
| TEST-21 | T21 / unit_contract | `node --test tests/gallery/*.test.mjs && pnpm test:fixtures` | Every card source/hash/output manifest validates; no unexecuted expectation presented as result; supported languages accurately labeled; gallery not padded with repetitive cases; all assets rights-cleared and <static limit. |
| TEST-22 | T22 / integration | `pnpm test:browser -- tests/reports/import.spec.ts` | Real selected export reopens without upload; source omission and unavailable reader explicit; malicious report cannot fetch/execute/select profile; hash mismatch blocks replay; comparison asks for exact locally selected source reports. |
| TEST-23 | T23 / integration | `pnpm test:browser -- tests/reports/selection.spec.ts` | Default selected finding does not include unrelated text/name/PDF/notes; full-source warning requires explicit choice; crop is described as excerpt not safe redaction; selected omissions retain honest original produced_count and check coverage. |
| TEST-24 | T24 / unit_contract | `node --test tests/security/import/*.test.mjs && python scripts/fuzz_reports.py --fixed-seed 8090 --cases 1000` | Controlled oversized/truncated/HTML/SVG/polyglot/prototype/path inputs fail before dangerous allocation or resource use; bounded fuzz receipts show no unexpected accepts/crashes/egress; HTML export opens script-free; no archive decompression surface. |
| TEST-25 | T25 / unit_contract | `pnpm test:privacy -- tests/privacy/cache.spec.ts` | Cold/offline without model reports unavailable; prepared offline own-file works without requests; model update cannot reuse wrong hash; clear terminates workers/revokes URLs/removes requested caches, with no forensic erasure promise. |
| TEST-26 | T26 / integration | `uv run --project native python -m pytest native/tests/readers -q` | Mapping/control/covered actual outputs preserved; shared glyph boxes not split with guessed widths; pypdf text stays page-only; all rotations/origins map correctly; malformed/encrypted failures isolated; build/version manifest complete. |
| TEST-27 | T27 / integration | `uv run --project native python -m pytest native/tests/ocr -q` | No URL or runtime model fetch; missing model/binary/unreadable page differ; malicious filename cannot become an option; crop padding/resize inverses correct; native OCR retains raw punctuation and all occurrences. |
| TEST-28 | T28 / integration | `uv run --project native python -m pytest native/tests/structure -q` | Invisible-over-border and white-on-dark controls cannot produce universal hidden/visible verdict; no false “all checked”; off-crop observation uses raw/native geometry while page display remains crop; unsupported compositing explicitly recorded. |
| TEST-29 | T29 / integration | `uv run --project native python -m pytest native/tests/runtime -q` | Injected Python/native-like hang/crash/OOM/output flood gets classified and bounded; successful files remain byte-stable; one retry max within deadline; empty success not retried; Ctrl-C terminates descendants and leaves valid partial artifact. |
| TEST-30 | T30 / integration | `uv run --project native python -m pytest native/tests/cli -q` | Representative documented commands execute; exit0/2/3/4/6/130 cases correct; output validates/reopens; replay refuses mismatched source/hash/profile; no arbitrary imported executable or silent reader substitution. |
| TEST-31 | T31 / integration | `node --test tests/bridge/*.test.mjs && uv run --project native python -m pytest native/tests/bridge -q` | Golden cross-language comparison cases agree exactly; missing Node yields clear capability error, not fallback algorithm; stdout bounded; no shell/plugin/profile selection from report; manifests retain comparator version. |
| TEST-32 | T32 / integration | `uv run --project native python -m pytest native/tests/corpus -q` | Duplicate filenames remain distinct corpus keys; traversal/symlink escape rejected; altered bytes invalidate resume; one crashing file cannot erase successful reports; deterministic index ordering and explicit partial counts survive interruption. |
| TEST-33 | T33 / integration | `uv run --project native python -m pytest native/tests/profiles -q` | Old/new pypdf versions demonstrably loaded from distinct interpreters; reader version printed in outputs; missing version install blocks that profile; untrusted profile path/command refused; offline run makes no requests. |
| TEST-34 | T34 / integration | `pnpm test:regression && uv run --project native python -m pytest native/tests/baselines -q` | Changed no-rule is informational; rule regression exits5; lost coverage cannot count improvement; incompatiblefiles/readerconfig marked; baseline overwrite/auto-refresh prohibited; new baseline requires explicit approval and new path. |
| TEST-35 | T35 / unit_contract | `python -m unittest discover -s tests/examples -v` | Example runs end-to-end locally; before/after identities differ as intended; known rule-failing mutation exits5 without modifying baseline; output reopens in web viewer; README commands copied verbatim into test. |
| TEST-36 | T36 / unit_contract | `python -m unittest discover -s tests/evaluation -v` | No sibling leakage; skipped/failed pages remain coverage denominator; repeated OCR readings are not independent samples; targets reported as unmet until real run; small real incident data stays permissioned and out of public logs. |
| TEST-37 | T37 / integration | `pnpm test:a11y && python scripts/check_manual_receipts.py accessibility` | All core flows completed without mouse; focus restored; amount alternatives and partial coverage announced correctly; 400% zoom usable; reduced motion honored; zero serious/critical automated and zero blocked manual critical flow. |
| TEST-38 | T38 / integration | `pnpm test:visual` | Approved screenshots show no clipping/overlap/empty fake controls; all interactive actions work; source page remains focal; no intimidating metrics wall; mobile view meaningful without promising heavy OCR everywhere. |
| TEST-39 | T39 / integration | `python scripts/measure_performance.py --profile reference-desktop && pnpm test:browser -- tests/performance/budgets.spec.ts` | Reference targets either pass with raw receipts or block affected capability; no invented portable JS heap cap; job/raster/output budgets enforced; mobile fallback is visible; peak/tail timings and device identity reported, not synthetic elapsed animation. |
| TEST-40 | T40 / integration | `uv run --project native python -m pytest native/tests/security -q && python tests/containment/run.py` | Source bytes unchanged; output cannot escape root; child/descendants terminated; successful unrelated files remain valid; unsupported OS limits reported; actual container no-network test uses owned synthetic files and records image digest. |
| TEST-41 | T41 / experiment | `python experiments/P09/run.py --manifest evaluation/manifests/development.json --out artifacts/P09 && python experiments/P09/test_experiment.py` | Zero hard-control false visibility; precision/coverage/runtime recorded; candidate gain measured without held-out leakage; negative result is complete; proposed merge has separate adapter capability review and full controls. |
| TEST-42 | T42 / experiment | `python experiments/P11/run.py --manifest evaluation/manifests/development.json --out artifacts/P11 && python experiments/P11/test_experiment.py` | >=20% useful target gains at<=1ppprecision loss or reject; clean neighbor fields not corrupted; every accepted transform source-bound; failed/modelmissing jobs remain denominator; full raw receipt retained. |
| TEST-43 | T43 / experiment | `python experiments/P12/check_manifest.py && pnpm test:browser -- experiments/P12/browser.spec.ts` | Useful complementary mechanism gain at fixed precision and bounded download/RSS or reject; API/geometry proven; every asset<24MiB; raw results, negative controls and rights included; default unavailable if candidate fails. |
| TEST-44 | T44 / experiment | `python experiments/P13/run.py --manifest evaluation/manifests/development.json --out artifacts/P13 && python experiments/P13/test_experiment.py` | Complementary useful failures at fixed precision and memory budget or reject; explicit missing-model and OOV behavior; no runtime network; no consensus-as-truth; full raw and negative outcomes documented. |
| TEST-45 | T45 / experiment | `python experiments/P14/run.py --manifest evaluation/manifests/development.json --out artifacts/P14 && python experiments/P14/test_experiment.py` | Any lost registration or clean corruption rejects; gain must transfer beyond the challenge generator; no generic reconstruction/search or PDF write path; negative result closes task with deleted production candidate. |
| TEST-46 | T46 / unit_contract | `node --test tests/parity/*.test.mjs && python scripts/check_manual_receipts.py compatibility` | No unexplained difference in promised semantic equivalence; required hash vectors same everywhere; engine renders not forced equal; exact build/CPU/OS/model manifest and all failed/skipped profiles recorded. |
| TEST-47 | T47 / unit_contract | `python scripts/check_distribution.py --release && python -m unittest discover -s tests/release -v` | No unknown-license/hash asset; no unpatched reachable critical/high risk without explicit reasoned review and disabled affected feature; license summaries agree with bundled notices; no fonts copied from planning environment or private input included. |
| TEST-48 | T48 / unit_contract | `pnpm build && python scripts/check_static_dist.py apps/web/dist --config wrangler.json && python -m unittest discover -s tests/deployment -v` | Actual build has only static assets; each asset<24MiB and rights and hash manifest complete; routes/deeplinks/missingassets expected; no third-party requests; wasm/CSP works without script unsafe-eval; default no COOP/COEP/SharedArrayBuffer requirement. |
| TEST-49 | T49 / unit_contract | `python -m unittest discover -s tests/docs -v && python scripts/check_claims.py` | Clean-machine instructions and all commands exercised; no unsupported “safe/fraud/AI sees”/score/agent autonomy claims; repeated-reader workflow documented; any unknown rights and owner inputs plainly separated; links valid. |
| TEST-50 | T50 / unit_contract | `python scripts/check_claims.py --surface launch && python -m unittest discover -s tests/docs -p test_launch_claims.py -v` | Each launch claim has source commit/test receipt; clipped time labeled; no fabricated adoption/scores/manual contribution; no sensitive document in media; final direct-try/source/limits links supplied only after owner publishes. |
| TEST-51 | T51 / gate | `pnpm exec playwright test tests/gates/g2.spec.ts` | All mandatory public features work without native/backend; all controls functional; missing-model and unsupported results remain partial, never clean; tests run against built artifact with exact versions; defects reopen owners. |
| TEST-52 | T52 / gate | `uv run --project native python -m pytest native/tests/gates -q` | Baselines never update automatically.; Stated rules detect regressions and lost coverage.; The compared versions actually run in separate, recorded environments.; One failed file cannot erase successful results from other files.; Reader, source and configuration identities survive report transfer.; The documented local CI example executes without a hosted account. |
| TEST-53 | T53 / gate | `python -m unittest discover -s tests/gates/quality -v && python scripts/check_manual_receipts.py release` | Numeric quality targets and critical invariants have actual receipts; all required profiles covered; no missing-rights exception; no skipped required tests; any product limitation reflected consistently in UI/docs/claims. |
| TEST-54 | T54 / gate | `pnpm exec playwright test tests/deployment/public.spec.ts` | The public sample and own-file workflow work on the actual authorized origin.; Deployment configuration, request-path tests and available account metrics establish that application traffic does not invoke a dynamic Worker.; No upload endpoint or account-injected analytics script is present.; Rollback serves the previously archived artifact and its verified hashes.; The archived distribution, model manifest, SBOM, source, test receipts and public claims match the release.; The owner records explicit publication approval without disclosing secrets. |
| TEST-55 | T55 / gate | `python -m unittest discover -s tests/gates/final -v` | All G1–G4 receipts are fresh for the actual release candidate.; Static deployment preflight and documentation command checks pass.; No public claim exceeds its linked evidence.; The owner records visual approval of the working application.; Only owner-controlled publication inputs remain before T54. Any defect creates a specific corrective task and invalidates the affected gate receipts. |
